table:
    source
    name: lmTestBootstrapSource
    columns: (class: class) (class: kind)
    rows:
        lmTestBootstrapCopied primitive
end: lmTestBootstrapSource

table:
    source
    name: lmTestBootstrap
    columns: (class: class) (class: kind)
    rows:
        lmTestBootstrapValue descriptor
end: lmTestBootstrap

join: (lmTestBootstrapSource) lmTestBootstrap
    source
    columns: (class: class) (class: kind)
    rows:
        lmTestBootstrapDirect relation
end: lmTestBootstrap
