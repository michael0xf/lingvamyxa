import: "lm2/own.lm2"

table:
    source
    name: `cast.target`
    columns: (class: class) (int: value)
    rows:
             void  0
             void  1
             void  None
end: `cast.target`

table:
    source
    name: `cast.target`
    columns: (class: class) (class: value)
    rows:
             LmOwnAllocationDescriptor  int
end: `cast.target`

table:
    source
    name: `lm.test.cast.target.seed`
    columns: (class: class) (int: marker) (int: target)
    rows:
             LmOwnPtrStack  7  1
end: `lm.test.cast.target.seed`

join: (`lm.test.cast.target.seed`) `lm.test.cast.target.middle`
    source
    columns: (class: class) (int: marker) (int: target)
    rows:
end: `lm.test.cast.target.middle`

join: (`lm.test.cast.target.middle`) `cast`
    source
    columns: (class: class) (int: marker) (int: target)
    rows:
end: `cast`

table:
    source
    name: `cast`
    columns: (class: class) (int: marker) (int: target)
    rows:
             LmOwnPtrStack  8  None
end: `cast`

external:
    fn: main () int
        int: value 7
        @: void direct cast: (@: void) @ value
        LmOwnAllocationDescriptor: fallback cast: LmOwnAllocationDescriptor direct
        LmOwnPtrStack: joined cast: LmOwnPtrStack direct
        return: direct = 0 || fallback = 0 || joined = 0
    end: main
end: external
