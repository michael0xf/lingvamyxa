Generated unit root binding and owned graph construction
=======================================================

l2trans's graph-unit path emits a synthetic l2_program_entry adapter and an
outer native main. Before constructing the unit, main creates one local Message
runtime and its process root using existing runtime_new/create/find APIs. Once
the unit's initial child block is constructed, set_graph records its exact
address in that Message. No node is rewritten and source-method signatures are
unchanged. The synthetic adapter is not an LMX method or a new implicit owner
parameter. On each ordinary adapter return, including construction failure,
main deletes the runtime and returns the original program status. Runtime/create/
lookup failure paths also release any successfully created runtime once.

run_l2trans links generated Message-using programs against real Message support,
never lmx_poll_stub. The translator and unrelated old harness programs keep the
stub where needed. Headers/modules are translated once and thirteen support
objects compiled once per runner invocation/source snapshot/flags. The same
objects serve compatible subsequent programs. No cross-run stale object cache.
BuildOnly exposes the existing translator build and functions without executing
the large historical suite; OutputDirectory and TranslatorPath allow a private
snapshot runner with the stable compiler kept read-only.

run_l2_message_root uses an immutable core git archive with the five current
owned implementation/driver files overlaid and hashed. It builds one translator,
translates unit_bool_and through L2 -> L1 -> C, links the real Message once, and
uses a test-only L1 driver for runtime-new/create/lookup failures, allocation
failure before and after graph binding, and successful execution with preserved
nonzero return value. Normal program and instrumented driver share support
objects; no compiler self-build or repeated Exec suite is needed.
The historical splice driver now inserts its body inside the synthetic adapter
and preserves the outer main/Message teardown. A focused spliced method call
checks this route and a zero exit using the same already-built support objects.

Verified 2026-09-11 against core be4bbddda51f4abc29e4fbc6da1b28f87f5e4f7e:
  build/codex/l2_message_root/20260911_185802_951_f9d34937/evidence.json
Six modes passed 58 assertions (13/5/9/9/9/13), normal program exit=1 and splice
exit=0/output=7. Strict C99 with the existing pointer/implicit-declaration guards;
the driver adds -Werror. No optimization flag was added to this existing runner.
All 17 recorded stages matched expected exits. Compiler/source/runner hashes,
archive hash and private artifact hashes are recorded; generated program/driver
C were inspected. Eleven cached support-object hashes stayed unchanged across
normal and splice links. Earlier setup attempts failed on a relative-path read,
unsupported multiple fn entries in one external block, and a missing generated
header include path; these were fixed before the final complete run. No full
historical suite, stable promotion, Message Exec rerun or self-build is claimed.

Owned branch checkpoint, 2026-09-11
---------------------------------
lmx_branch_owned.h.lm1/.lm1 is a separate header/module, leaving the legacy
lmx_branch predef and its standalone link dependencies untouched. Both generated
construction sites (unit children and supported nested for-body children) call
lmx_branch_open_owned(parent, children, &msg->blocks, &msg->ranges). All emitted
closed-unit child accesses use lmx_branch_child_known: null/bounds checks over
the compiler-proven Structure layout, not a global classifier or runtime owner
search. Source-method signatures and dirty-only publication are unchanged.

The constructor requires an empty parent and exclusive owner access. It checks
zero/overflow/null inputs, prepares one nonmoving child allocation containing its
range record, and a separate free-compatible block-list node. The child array
offset is a multiple of sizeof(Lmx), preserving malloc alignment without C11
alignof or flexible-array extensions. Paired storage_move_all admission precedes
publication of parent.data/len. Allocation or admission failure frees temporary
storage and leaves both destination lists and the parent unchanged. New children
are initialized with their actual lexical parent; no existing node is rewritten.
These child blocks have no global registration or dual-registration. Message
cleanup retires their embedded ranges before releasing payloads and block nodes.
Do not pass this storage to the legacy branch_release function.

Verified against core 872e33348cda8d312efd4b2c000246413de98f1d:
  build/codex/l2_message_root/20260911_191031_976_408f2939/evidence.json
Nine modes passed 170 assertions (26/5/10/10/10/26/12/13/58), including both
constructor allocation failures, rejection of malformed destination metadata,
unchanged prior ownership, successful retry, stable child addresses/node links,
empty/nonempty/overflow/null/bounds guards, and one-time release of all tracked
branch allocations. Runtime failure/early-return checks remain in the same driver.
Normal program exit=1; splice exit=0/output=7; existing unit_forj_again preserves
output 0 then 9 and exit=0 through two owned construction sites. All 23 stages
matched expected exits. Twelve support objects were built once and their hashes
stayed unchanged for all compatible links. Strict C99, existing guards, driver
-Werror; no explicit optimization flag. Generated helper/header, driver and unit
output were inspected. The first setup compile exposed an undeclared uchar alias
in the new helper; it now uses standard char byte arithmetic. A direct L1 syntax
check also corrected test aggregate declarations before the complete run.

Owned root and METHOD checkpoint, 2026-09-11
------------------------------------------
lmx_value_owned.h.lm1/.lm1 adds fresh typed construction with existing Lmx and
LmxMethod layouts. lmx_node_new_owned admits a zero-initialized root occurrence;
lmx_method_new_owned admits an initially empty METHOD record, which its caller
fills before publishing into the graph. These are construction APIs under
exclusive owner access, not ways to mutate existing callable records.

Like branch construction, each prepares a nonmoving payload with embedded range
metadata plus a separately free-compatible block node. The payload offset is a
multiple of the concrete type's size. Null owner slots and size overflow reject
before allocation. Allocation/admission failure releases temporary allocations
and preserves both owner list heads. The public header exposes only the two
concrete constructors, not arbitrary runtime type admission.

Generated unit and METHOD construction now use these owner-head APIs instead
of raw malloc/global registration. METHOD publication no longer consults the
global classifier. After an initial branch failure, the outer Message cleanup
releases the already-owned root; no raw free(unit) remains. Ordinary completion
also releases root/METHOD allocations through Message storage. Lexical node and
source-method ABI are unchanged. Stable typed addresses/range metadata travel
together through existing storage_move_all; no owner lookup is introduced.

Verified against core 06424214eab3a15608802acd9b1304b671cf3bea:
  build/codex/l2_message_root/20260911_193110_073_49a8d116/evidence.json
One private build with five current overlays, thirteen cached support objects,
27 stages and 325 assertions across thirteen modes:
46/5/11/11/11/38/21/22/58/12/13/39/38. Both malloc sites of both new constructors
were fault-injected, including root failure before binding and METHOD failure
after binding. Owner-local classification, null input, malformed destination,
temporary cleanup, retry, storage transfer with unchanged node/address/payload,
and one-time release of every tracked root/METHOD/branch allocation were checked.
Mode4 still covers the remaining legacy range initialization failure; mode5 now
explicitly targets first METHOD allocation. Earlier branch contract tests remain.
Normal exit=1; splice output7/exit0; nested for-body output0 then9/exit0. One
translator and unchanged support-object hashes served all compatible programs.
Strict C99/default flags, driver -Werror; no extra optimization flag. Generated
new helper/header, entry adapter and wrapping code were inspected. No full
historical suite, stable replacement or completed self-hosting is claimed.

Owned int and size_t checkpoint, 2026-09-11
-----------------------------------------
The same value module now constructs zero-initialized, unique int and size_t
cells in Message storage with existing PRIMITIVE/INT/SIZE_T tags. Updates retain
the cell address. Known typed read/store helpers guard null and operate on the
compiler-proven live cell type, not on an arbitrary raw address and not through
a global type/owner lookup. These preconditions are explicit in the header.

Both generated field-construction sites, own-cache loads, dirty-only publication
and explicit graph paths use the new primitives. No int/size_t pool initialization
is emitted; ordinary units no longer import the legacy int/size_t pool modules.
Source-method signatures, same-name argument binding, actuals-before-checkpoint,
no-reload and graph-address semantics are unchanged. Interned characters remain
on the legacy intern-by-value path; this is NOT a change to character semantics.

Historical splice bodies are adapted only in the synthetic graph-entry route:
known child/primitive accessors and requested int/size_t construction use owned
storage there. Ordinary legacy standalone routes are not globally rewritten.
The old mandatory global METHOD-classifier assertion and views pool-take assertion
now check owner-local construction. No whole historical suite was rerun here.

Final evidence against core c557908e424b0bb00945fa407ae232729df09502:
  build/codex/l2_message_root/20260911_193855_967_bfc5afc5/evidence.json
Five current overlays, 50 stages, 18 driver modes / 572 assertions. The five new
primitive modes contribute 47/49/50/50/51 checks: both allocation failure sites
for each primitive, retry without lost prior cells/ranges, zero initialization,
distinct mutable cells, signed-int bounds, full size_t width, direct typed-address
reads/writes, transfer with stable payload/address, null guards and complete
release of tracked allocations. Earlier root/METHOD/branch checks remain.

Six existing source regressions share the one private translator and thirteen
unchanged support objects: forj_stale -> 9 then '9 42'; node_path -> '0 1';
addr_take -> 0 then1; bind_sz -> 3 then3; asgn_bind_sz -> '9 10' (source cell
unchanged); own_same_name -> '1 2' then '2147483648 2', with distinct addresses.
No legacy int/size_t calls in emitted L1 or pool globals in these generated C
units. Normal/splice/nested entry checks also pass. Strict C99/default flags,
driver -Werror; helper and graph-address emission inspected. An earlier passing
run preceded the historical-adapter/runner assertion updates; this final run
records the complete current source hashes. Stable compiler stayed read-only.

Closed-unit legacy catalog removal, 2026-09-11
-------------------------------------------
Units with no char/P0/query dependency no longer emit the legacy branch/pool
predef or initialize the old range catalog. Their root, child, METHOD, int and
size_t operations already use owner-local storage and compiler-proven access.
Char/P0/query units retain the still-required legacy dependency; this is not an
implicit change to character interning or foreign adapter semantics.

Mode4's old legacy-range allocation fault no longer has an allocation to target.
It is replaced, not faked: the generated program runs twice sequentially, creating
and releasing a fresh process Message each time. Both passes must actually bind
and execute; the return code alone could otherwise disguise an early failure.
All tracked graph allocations are released on both runs. Existing owned-allocation
failure cases remain. The size_t caller-graph historical driver now constructs
its extra source graph with owned helpers rather than reimporting the old catalog.

Verified against core eaac7c56f8df36faadaae329f66d505b3280c486:
  build/codex/l2_message_root/20260911_194421_469_5fd01176/evidence.json
53 stages, eighteen modes / 649 assertions (new mode4:88 checks). Source/emitted-C
checks reject legacy range initialization/pool imports and catalog/pool symbols
in the closed fixtures. All previous numeric, graph-address, binding, dirty-only,
splice and nested tests retain their expected outputs. One additional existing
unit_printf_char fixture still initializes its legacy char dependencies and prints
65; this checks the conditional boundary without claiming char migration.
One private translator/thirteen unchanged cached support objects, strict C99/
default flags, driver -Werror. Generated closed-unit C was inspected. No full
historical suite or stable promotion was run.

Signature diagnostics without runtime state, 2026-09-11
-----------------------------------------------------
The emitted l2_sig_f0/f1 and l2_intern_id/again/swap/probe values are now
comment-only translation evidence, not file-scope C variables. Existing textual
contract inspections retain the same names/values. No hidden shared table,
Message field or extra allocation replaces these diagnostics.

Canonical signature equality is still proved by l2_intern_prove during
translation. Actual METHOD records keep the same sig and callable ABI. The
generated entry's repeated-intern comparison uses the known emitted integers;
its nonempty-first-formal guard uses that validated identifier's known length.
Neither guard reads mutable diagnostic state or performs runtime name lookup.
The source argument names are validated identifiers, so the length-zero test
preserves the previous empty-first-character check. No character interning,
dynamic-through binding, lexical node or dirty-only rule changes here.

Saved PASS against the same isolated core eaac7c5 and five current overlays:
  build/codex/l2_message_root/20260911_201312_056_28f84f69/evidence.json
57 successful stages; existing eighteen modes / 649 assertions, including two
fresh process Messages. All prior primitive/binding/graph-address/nested/spliced
outputs remain unchanged. New assertions require all six diagnostic entries to
be comments in generated L1 and absent from generated C, including char units.
Four existing add/renamed-method/different-formals/swapped-formals fixtures verify
preserved comment evidence, canonical identity/reintern/probe relationships and
METHOD sig. These four need translation only; no additional support-object build.

One private translator and thirteen unchanged support objects were reused,
stable L1 read-only, strict C99/default flags and driver Werror as before.
The saved source hashes were rechecked after the run. This checkpoint deliberately
does not claim the latest collector integration was rebuilt: Grok's separate
0ce7957 runtime integration and saved Exec PASS were reviewed read-only.

Own-name diagnostics without runtime state, 2026-09-11
----------------------------------------------------
The emitted l2_ownN names are also comment-only translation evidence. Their
source spelling and index order remain available to existing textual OwnUsed
checks. This removes their file-scope pointer definitions, not actual fields,
typed caches, dirty flags, graph storage or source visibility.

Saved PASS:
  build/codex/l2_message_root/20260911_202123_402_9ec9fbcb/evidence.json
Same isolated eaac7c5 base/five overlays and stable compiler pin. 63 successful
stages; previous eighteen driver modes / 649 assertions and all outputs pass.
Generated C is checked for absence of own-name as well as signature-diagnostic
symbols. Existing unit_own5 and unit_own6 run their actual method bodies through
the synthetic Message entry; metadata is checked for all five/six original names
in order. The mixed char/size_t fixture keeps its existing char intern path.
One private translator/thirteen unchanged support objects, no self-build rerun.

The added silent-output fixtures exposed an empty-file handling bug in the
focused PowerShell runner. Explicit null handling now accepts genuinely empty
stdout; it does not hide a failed exit or a mismatch against nonempty expected
output. Two earlier runs failed in that harness check, not in the translated
method. Only the final path above is accepted evidence.

Known-typed character reads, 2026-09-11
-------------------------------------
lmx_char_value_known in lmx_value_owned requires a compiler-proven live char
cell; null returns -1, otherwise the result is its unsigned byte (0..255).
It reads without a type registry, owner search or allocation. It is not an
arbitrary-pointer validator. The existing interned cells, lmx_char_cell,
initialization and write/publication behavior are unchanged.

The graph-unit compiler uses this helper at three statically typed read sites:
hidden-argument graph fallback, explicit own-field path, and own-cache load.
The test drive adapter uses the same helper for known fixture char layouts.

PASS build/codex/l2_message_root/20260911_210611_852_e08aab70/evidence.json:
the same isolated eaac7c5 snapshot with five owned source overlays, one private
translator and thirteen reused support objects. Nineteen driver modes pass:
the prior 649 assertions plus 259 in the new null/all-256-byte mode. This mode
uses an unregistered live char local, proving the reader needs no type catalog.
Existing char early-return, clean-caller and dirty-RHS outputs pass unchanged;
an isolated L2 char explicit-path fixture returns 65/65 and generated L1 proves
both path/cache reads use the new helper. Generated method C is checked for
absence of calls to classified character reads. Source/toolchain/configuration
and artifact hashes are saved. Stable compiler remained unchanged.

The preceding 210444_686_aef303f0 run failed only in a new C-definition matcher
that incorrectly excluded body semicolons; its passing driver is not a full
acceptance result. The final run above includes the corrected matcher and passes.
This slice does not migrate char ownership or verify the latest Grok collector.

Text/query dependency removal, 2026-09-11
---------------------------------------
l2_text_hash no longer imports lmx_size and its unused branch/char/range/pool
implementation chain. Its FNV, borrowed query fill and byte comparison bodies
are unchanged. Graph-unit emission imports legacy branch/chars and initializes
the legacy range catalog exactly when an actual own char field needs them,
including mixed text/query/char units. No new lifetime or owner ABI is added.

PASS build/codex/l2_message_root/20260911_211950_819_818fdf57/evidence.json:
88 successful stages, nineteen driver modes / 908 assertions, all prior outputs.
One immutable eaac7c5 snapshot with six hashed source overlays, one private
translator and thirteen reused support objects; the stable compiler is unchanged.
Four new private fixtures derive from the archived parser_text_views source:
P0-only, query-only, combined views and combined views plus a real char field.
They check equality/length/nulls, borrowed payload pointer/length, query invalid
state and borrowed pointer/hash, current text mutation, forced hash collision
rejection and mixed-unit char result 65. Generated char-free C is checked for
absence of the legacy range catalog and char/int/size pools. Mixed units retain
char initialization and known-typed reads. No run_l2trans linkage edit was needed.
This is focused adapter/emission evidence, not a full parser/self-build run or
integration test of Grok's newer collector. The char owner-local constructor is
still a separate helper awaiting explicit Message/context integration.

Message-owned character emission, 2026-09-11
-------------------------------------------
Grok confirmed exact files and same-table rebind semantics in 212500. Graph-unit
entry now constructs a complete immutable char table directly in Message-owned
blocks/ranges. Own and nested-field initialization publishes its zero cell;
cache, aliased-parameter and explicit-path writes use lmx_char_rebind_known
on the target field's existing live table cell. Reads stay known-typed. No
source signature, dynamic selection, dirty-only checkpoint or lexical link
changes. No Message singleton, owner search, new graph field or TLS was added.
Removed the final graph-unit legacy branch/char import, range/char init and
obsolete range-capacity calculation. This does not alter the legacy leaf emitter.

PASS build/codex/l2_message_root/20260911_212942_437_6cc83220/evidence.json:
105 successful stages, 24 driver modes / 2135 assertions. One eaac7c5 snapshot
with eight hashed overlays, one private translator, fourteen reused support
objects (adds lmx_chars_owned). Two simultaneously live generated Messages
have distinct character cells and owner ranges. Injected failures at both
char-table allocations and later root/METHOD allocations release every tracked
table/graph allocation exactly once. Existing nineteen driver modes pass.
Generated C for all focused char and non-char cases has no legacy range catalog,
char/int/size pools, chars_init or char_cell calls. The old read-only char helper
also retains its arbitrary-live-byte contract; rebind has stronger provenance.

All prior outputs pass, including clean-caller/dirty-RHS, early return, explicit
path, mixed query and nested char initialization. Three historical drives
unit_asgn_bind, unit_dyn_predecl and unit_bind now also run through the focused
runner, covering aliased writes and distinct generated nodes. The drive adapter
uses the entry's table for fixture cell creation; its two old raw branch fixtures
now use owned node/branch construction. The full historical runner still has
older unrelated gates and was not run or claimed as passing here.

The prior 212837_719_6853eb06 run passed the initial integration; the final run
above additionally covers these historical drive compatibility changes and LF
normalization. The helper's separate 67892-check PASS is in LMX_CHARS_OWNED.txt.
Stable compiler unchanged. No newest-collector/end_turn/ARRAY/self-build claim.

Narrow foreign allocation predef, 2026-09-11
------------------------------------------
l2_need_own imports only l2_foreign_alloc.lm1's four admitted allocation/copy/
resize/delete calls. This removes unrelated L1 pointer-stack/absorb code and
three mutable test counters from generated units. Ordinary caller-managed
foreign allocation/default callback behavior is preserved; no Message ownership
or lifetime is implied. L1 own/parser and their test globals remain unchanged.

PASS build/codex/l2_message_root/20260911_213949_720_0ffec604/evidence.json:
111 stages, 24 modes/2135 driver assertions, eaac7c5 plus nine hashed overlays,
one translator/fourteen unchanged support objects. Real parser_text_heap and
a resize fixture using archived parser_indent_stack typed accessors pass,
including borrowed alias/mutation, copy termination, deletion, grow preserving17
and zero-size free. All prior outputs pass. Generated C excludes the old
allocator counters and unrelated helpers. The separate 44-assertion native
failure/destructor test is documented in L2_FOREIGN_ALLOC.txt, along with the
initial unsupported-index fixture failure and the final corrected evidence.

Remaining limitations (not fixed or blessed by these checkpoints)
---------------------------------------------------------------
Older leaf-adapter paths remain outside this checkpoint.
Other foreign predefs may still bring in old definitions. Signature and own-name
diagnostics no longer create runtime state; the L1 translator itself still has
compiler state. This is NOT proof that all globals in the full toolchain are gone.
There is no claim that all remaining generated global state or allocations are
gone. Root/METHOD/CHILDREN/int/size_t owner admission is NOT evidence of complete
graph collectibility, reference-ARRAY admission or full allocation cleanup. These
checkpoints add no owner-link, TLS owner lookup, global registry, arena relocation
or scheduler.
It does not make the generated main a scheduled/bound turn, implement full
delivery/merge, or establish completed L2 self-hosting. The older leaf-adapter
translation path is also separate from graph-unit emission.

Positive own-array declarations, 2026-09-11 (Grok221300 / Codex221600)
-----------------------------------------------------------------
The graph compiler now admits method-body declarations []: int NAME COUNT
and []: char NAME COUNT at the body's top level, with a positive decimal
literal count. Own metadata type4/5 denotes INT/CHAR descriptors; 0/1/2 keep
their prior meaning. The extent is retained with the declaration metadata,
copied on capacity growth and released with that metadata. Decimal overflow
is rejected; leading zeros normalize to decimal (003 emits3U, never octal).

Existing graph layout gives the field an ordinary unit-child Lmx occurrence.
The synthetic runtime entry calls lmx_array_new_positive_owned with that
Message's block/range heads and publishes the descriptor to leaf.data. The
descriptor remains len/data, backing remains contiguous primitive storage.
Constructor failure returns1 from entry; outer main deletes its runtime.
There is no scalar dirty cache or intern-table rebind for these array fields.
The source fixture contains INT3 and CHAR4 and executes m(7), returning0.
c.array remains the existing foreign local buffer form, unchanged.

This is creation/placement only in the existing synthetic-entry profile.
Indexing, assignment/rebinding, array-valued formals/results, nested own-array
declarations, rank2, initializers, dynamic/zero extents, views and reference
arrays are outside this slice and rejected where tested. It does not decide
the source empty-array ABI or provide full per-activation constructor semantics.

PASS build/codex/l2_message_root/20260911_222127_766_c9e25dab/evidence.json:
197 stages,34 driver modes/2693 assertions;55 compiler allocator failures and
15 rejected source cases included. One private translator, eaac7c5 plus12
hashed overlays,15 reused support objects (adds existing lmx_array_owned).
Actual L2->L1->C execution: descriptor/backing types, lengths, zero elements,
field publication; all8 array allocation faults plus later METHOD failure;
caller lists unchanged on constructor failure and all tracked resources freed.
Six-array metadata growth preserves every extent; all55 injected compiler
allocation failures leave live=0 and preserve an existing destination file.
All previous char/foreign/branch/cache/signature fixture outputs still pass.
Generated C retains existing unused-variable/parameter warnings; native type/
declaration guards and the strict driver check pass. Stable compiler unchanged.

Initial221928 negative test found scalar-array assignment silently admitted;
221959 found explicit node-path read admitted. Validation now rejects both;
222017 passed before adding metadata growth/OOM evidence. Final222127 above
includes those checks. No colleague's active runtime or app build was rebuilt;
no full current collector/scheduler/bootstrap/self-hosting acceptance claimed.

Proven literal INT indexing, 2026-09-11 (Grok225400)
-------------------------------------------------
The same top-level fixed-positive-extent own INT arrays now admit literal
indexed reads and writes. Validate decimal syntax/overflow and index < extent
at translation, normalizing leading zeros. Emit ordinary unit-child access,
cast its existing descriptor/backing to their known types, then load/store the
contiguous int cell. Loads are captured as typed scalar temporaries; no dirty
own-scalar cache, allocation, descriptor rebinding or owner registry is added.
The c.printf argument emitter uses the same indexed load path.

This is a compiler subset proved in bounds, not dynamic bounds checking.
General Array OOB means throw: Bounds per specification; dynamic, negative,
out-of-range, suffixed/overflowed indices, rank2, views, element addresses and
CHAR accesses remain rejected. Existing array-reference/escape/rebind and
construction restrictions remain. Proven accesses rely on the existing closed
unit graph and fixed descriptor contract; no foreign mutation is admitted.

PASS build/codex/l2_message_root/20260911_230127_765_2589b4ec/evidence.json:
224 stages,35 modes/2752 driver assertions,13 overlays on eaac7c5,15 support
objects reused. Actual source program writes first/last cells to7/9, returns16,
and teardown inspects7/0/9 plus the typed descriptor before releasing all
tracked storage. Exact first-cell fixture returns7; cell-to-cell RHS returns23;
c.printf receives9 and the method returns16. Fourteen new unsupported-index
cases plus the prior15 array rejection cases pass. All55 existing compiler
allocator failures and earlier focused outputs remain green. Stable unchanged.

First pass found rank2 validation missing; next pass exposed only the driver's
old maximum mode33. Both corrected. Final run also covers the separate ccall
emitter. No full scheduler/activation/latest-collector/self-host claim follows;
runtime integration must compile emitted code against its matching headers,
not reuse an object built with an older Message layout.

CHAR literal indexing, 2026-09-11 (Grok231000)
-------------------------------------------
The same proof and emission path now accepts the existing CHAR own-array type.
Select native char pointers and char load temporaries instead of int pointers
and int temporaries; descriptor and backing layout do not change. Source char
follows the existing C char bootstrap mapping. This changes no text encoding
or native char signedness policy. Mutable CHAR backing is never intern-table
provenance and does not use lmx_char_rebind_known or lmx_chars_new_owned.

PASS build/codex/l2_message_root/20260911_231614_144_a6b3bf90/evidence.json:
237 stages,36 modes/2812 driver assertions,55 compiler allocator-failure cases.
actual source writes ASCII65/62 into first/last elements, returns127, leaves
middle byte0; teardown verifies ARRAY_OF_CHAR descriptor and stride1 CHAR
backing and releases all tracked storage. Separate c.printf case prints62,
checking native vararg promotion. Six CHAR dynamic/negative/out-of-bounds load
and store cases reject. Existing INT/constructor/allocation-fault and focused
regression cases pass unchanged (former CHAR rejection becomes reference-type
rejection). One private translator,14 overlays,15 support objects reused.
Stable compiler unchanged; no dynamic Bounds/catch ABI, resize, reference
indexing, latest runtime scheduling or full selfhosting claim.

Live own-array length, 2026-09-11 (Grok232200)
-------------------------------------------
length(own_array) now consumes one existing top-level INT/CHAR array reference
and returns the selected descriptor's live size_t len. Only this builtin form
admits the array argument; it does not expose or return the descriptor. A source
method named length wins normal resolution, so its declared argument/result
contract and behavior remain unchanged. No new runtime support object required.

PASS build/codex/l2_message_root/20260911_232721_400_ce47fb1b/evidence.json:
250stages/37modes/2888driver assertions,15overlays/15supportobjects reused and
55compiler allocator failures. Source INT3/CHAR4 returns7. Native test temporarily
shortens the live descriptors to2/1 and calls the same generated size_t method,
which returns3; lengths are restored before cleanup. This is a test of actual
metadata reads, not an admitted source resize operation. Generated L1 is also
checked for both descriptor len reads and size_t temporaries. Owned storage
cleanup and absence of an intern table remain checked. A declared length(z)
returns z+2 normally (9 for7). Six invalid builtin argument/arity cases reject.
All prior primitive array/index/foreign/own-field focused cases pass. Stable
compiler unchanged; no arbitrary reference length, source mutation, Bounds
throw/catch/result ABI or full selfhosting acceptance follows.
