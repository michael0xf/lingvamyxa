# The address of an own Array element inside a larger expression is refused
# the same way (run_l2_message_root's generated case).

fn: sum_address () int
    []: int buf 3
    buf[0]: 1
    buf[1]: 2
    buf[2]: 3
    return: @ buf[0] + buf[1] + buf[2]
end: sum_address
