Removing every lock but the message collection's own, and a full arena per Message (d6, draft, 2026-09-15, on c067bed9)

SCOPE (Mikhail, 2026-09-15, verbatim, on main 657ce3f9/07ad3679/668a5347)
- "Немедленно всё останавливайте и все усилия чтобы убрать эту общую блокировку. Она не нужна."
- "блокировка только локальная у коллекции сообщений может быть, больше блокировокне требуется даже локальных -- их
  в проекте не длолжно быть больше вообще"
- "значит реализуйте полноценную арену для каждого MEssage, как описано в спеке. Без этого Message не имеет смысла"
- On wait signals: "каких сигналов ожидания? снятие блокировки с сигналов ожидания чем-то может нарушить смысл этих
  сигналов? очевидно нет"
- "Message не имеет никаких ссылок на общие ресурсы кроме тех которые неизменяемые (independents: const: immutable:
  хранятся в неизменяемых массивах у root), либо на атомарные, для которых очередность не важна; очередь сообщений --
  синхронизированная коллекция"
- "Исполнитель сам освобождает свою память"; "У нас у каждого L3 Thread свой планировщик и сборщик мусора"
This file is a design, not the spec. Nothing in it is a rule until Mikhail agrees. The map's open questions 5.1 and
5.3-5.6 are his (LOCK_REMOVAL_SPEC_MAP.txt on main); a stage that depends on one names it and waits.

INPUTS (all on origin, all counted against a grep)
- exec.c lock sites: d6/lock-removal 39435ed2, LOCK_REMOVAL_INVENTORY.txt, 165 lines, 30 silent.
- exec.c allocations: 9e26a8c3, LOCK_REMOVAL_ALLOC_INVENTORY.txt, 75 lines, all on the C heap, none from an arena.
- Lookup spine: 9e26a8c3, LOCK_REMOVAL_SPINE.txt. 97 address lookups (exec.c 58, lm1 39) by the capability the caller
  holds: C1 the parent's child pointer, C2 the taking lane's own record, C3 a sender's held capability, C4 a child's
  parent capability (spec 11442), C5 R0's child list. N: 24 address queries from outside any Message.
- lm1/lm2 lock sites: sonnet/lock-inventory df360bb0, 72 sites (65 exec-lock family, 1 host_lock, 6 mail_lock), the
  6 mail_lock sites allowed.
- Tests, runners, oracle: claude-0c/lock-inventory e71e64b8, 34 lines. Measured: -LaneCheck does not go red when a
  lane write moves to a thread with no turn; 57's tripwire closes that (ruled (b)).

WHAT THE LOCK DOES TODAY (from the inventories, not from a rule)
L1. Address resolution. A walk from rt->root through every child list turns an LmxMsgAddr into a record
    (msg_find_any_locked, lmx_msg_find, self_or_find). The lock keeps the tree still under the walk. This is 35 of
    exec.c's 41 silent 5.1 lines.
L2. The family tree's own writes: child_link, child_chain_remove, the orphan re-rooting, the root-list unlink in
    endp_try_retire and release_slot.
L3. The execution mapping's cells: exec_bind record fields, mapped, launching, the bind wait's generation.
L4. Control flags read and written across lanes: ready, closing, running, handoff_ready, native_users.
L5. Runtime-level lists: rt->slots/n, e->retire_head, e->reap_head, rt->transport, e->ui_lane.
L6. Wakes: exec_wake_locked signals every worker after any turn; exec_ready routes one wake.
L7. Storage moves at settle: adopt_mark, dispose_mark, transfer_adopted, transfer_graph.
L8. Test setters and counters.

STAGES (each: red first, the change, green; the full gate on its integration merge)
Every stage runs run_gates N of N, run_port_message (the executor selftest), run_model_scenario36 cold, and 57's lane
tripwire once it is measured. A stage that removes lock calls also runs the UAF hunt kit's load shapes for 165 clean
runs (the kit's number).

S0. The proof tool (57): the lane tripwire red on a lane write from a thread with no turn. It is the proof for S2-S5
    and lands first.

S1. Dead code and the order's direct deletions (no question open).
    - Delete adopt_push (exec.c 3628, unused; its only occurrence is its definition).
    - Red: none needed for dead code. The proof is the grep at 0 and the gates green.
    - NOT dead, corrected 2026-09-15: lm2/own.lm2's native mutex and condition wrappers (lm_native_mutex_* /
      lm_native_condition_*) are called by own.lm2's public lm_mutex_* / lm_condition_* (2808-2885), and those by a
      whole lock-based message-thread runtime in own.lm2 itself: runtime\route_mutex, pool\mutex,
      pool\work_ready, thread\state_mutex, thread\stopped_condition (lines 581-1688 and on), plus own_abi.lm2 (41)
      and tests/message_thread_*.c and thread_provider_native.c (C, no runner found by grep). The lead's earlier
      "no caller" came from a grep that counted own.lm2 twice under two paths. Mikhail's answer ("Да всё удаляйте
      потому что это совершенно очевидно лишнее. Одна арена на Message, арены присоединяются при потреблении -- это
      ядро") was given to the wrong premise and is put to him again with the real extent. Out of S1: this runtime
      needs its own inventory (locks, conditions, threads, allocations) under the same order.

S2. Capabilities instead of address lookups inside the runtime (L1), locking unchanged.
    Every call site of the spine's C1, C2 and C5 takes the record its caller already holds. The site no longer
    resolves an address: run_one and the context worker use the record the take handed them (C2); bind, map, unbind,
    settle and step use the parent's child pointer (C1); R0's sweep uses R0's child list (C5). C3 (send) and C4
    (liveness toward the parent) change in S4, where the capability's form is decided.
    - Red: a test-build counter in lmx_msg_find and msg_find_any_locked, reported per caller class. It runs the
      executor selftest and the model tests, and asserts 0 lookups from C1/C2/C5 sites. Red today with the counts
      per site; it gets a tripwire run with one site put back.
    - Green: those counts at 0, the inventories' lookup lines for C1/C2/C5 gone, the lock still in place.

S3. One wake per lane (L6), no lock around a signal (Mikhail on wait signals).
    exec_wake_locked, which signals every worker after any turn, goes. A turn's end wakes only the lanes it readied,
    through each readied Message's own wake primitive. exec_ready signals without taking the lock.
    - Red: two mapped workers A and B. Readying A must not wake B. A test-build counter of wake returns that took
      nothing is nonzero for B today (run_one 2587 wakes all).
    - Green: B's empty-wake count at 0 over the same run; the executor selftest's wait tests green.

M. The sequential mapping removed (X), after S3 and before S4.
    Mikhail on 5.5, verbatim (2026-09-15): "мы же обсудили миллион раз. Закрытие родителя это авария. _Каждый ребенок
    закрывает себя сам и для его детей это такая же авария_. Если дети L3 Thread но реализация такая что у них нет
    своего потока, а они последовательно выполняются, то это нюансы реализации. Разумеется в этом случае закрытие
    родителя затягивается так как планировщик должен пройтись и выполнить чужую работу -- но зачем вам вообще сейчас
    такие потоки если вы не можете даже ядро архитектуры запилить точно по спеке?". The reading "for now every L3
    Thread runs on its own OS thread; the sequential mapping is removed until the core is exact by the spec" was put
    to him and he answered "ДА" (recorded on main by the coordinator, spec 19.28.R2.2 and model section 29).
    A child is launched on its own thread or not at all.
    What goes (surface counted by grep on cf326822):
    - lmx_msg_run_child_turn and child_turn_core's step path (exec.c, declared in lmx_message.h/exec.h, lm1/lm2 1
      each); lmx_msg_sched_step (lm1) and the scheduler record's cursor (lmx_sched_record.lm2
      lmx_sched_record_cursor/set_cursor); exec_bind_mode's launch = 0 host mapping.
    - The UI lane's stepping of UI-bound children: lmx_msg_exec_ui_step, lmx_msg_exec_take_ui_locked,
      lmx_msg_exec_take_addr, the MAP request (ui_request_locked, ui_request_if_ready_locked, ui_pending) and
      LMX_MSG_AFFINITY_UI as a bind affinity (exec.c 19 uses, the executor selftest 31).
    - What stays: R0's own turn on the bootstrap thread (lmx_msg_root_turn, lmx_msg_run_entry_turn). R0 is the
      bootstrap thread's own L3 Thread, not a child stepped by another thread; lmx_msg_map_child, which launches a
      child on its own thread.
    The UI lane without stepping: an L3 Thread whose lane is the UI thread, which takes its own Messages from its own
    mailbox. 19.28.R2.2 as written: "A request to another lane (mapping a child onto the UI lane) is a Message to that
    lane's mailbox" (11503-11506). Work a Message wants done on the UI thread is a Message sent to the UI lane. No
    other Message's turn runs on the UI thread. Proposed here, for Mikhail's agreement before any doc carries it.
    Tests migrated onto turn_step_child / run_child_turn in stage 5 (d), by name (grep counts of
    turn_step_child / run_child_turn / root_turn):
      lmx_model_family_close_32_selftest (3/1/1), lmx_model_family_release_17_selftest (7/2/4),
      lmx_model_liveness_33_selftest (4/1/4), lmx_model_orphan_mapped_17_selftest (5/1/2),
      lmx_model_root_ingress_5b_selftest (2/1/1), lmx_model_root_record_5e_selftest (2/1/3),
      lmx_model_scenario36_selftest (3/1/1), lmx_msg_adopt_unrooted_selftest (2/1/1),
      lmx_msg_family_handoff_selftest (4/1/1), lmx_msg_send_local_selftest (6/1/2, plus sched_step 1),
      lmx_sched_record_selftest (2/1/1, plus sched_step 1), tests/cancel_spin_host.c (4/1/2, sched_step 2),
      lmx_message_exec_selftest.c (24/5/14, sched_step 3, exec_ui_step 2, AFFINITY_UI 31),
      lmx_model_ui_lane_3d_selftest (exec_ui_step 1, AFFINITY_UI 1), and the runner run_turn_step_child_copies.ps1.
    Each becomes a test of a child on its own thread (the child's turn is the property, run on its context worker
    through map_child), or it is deleted with the property it checked named in the commit. Decided per test in M's
    commit after reading it. The rule for the decision: a property of the sequential mapping itself (round-robin
    order, the cursor, the UI take) is deleted; a property of the child (its close, its settle, its mail) moves
    onto its own thread.
    - Red: an S2-style test-build counter of step entries (child_turn_core with bootstrap = 0, sched_step,
      exec_ui_step), nonzero over the executor selftest and the model tests today.
    - Green: the functions deleted (grep at 0 for run_child_turn, sched_step, exec_ui_step, turn_step_child,
      AFFINITY_UI), every converted test green on its own thread, run_port_message -LaneCheck with the S0 tripwire.
    Effect on the other stages: S2's green loses the C1 walks from run_child_turn and sched_step, which go with the
    mapping instead of being threaded. S4 no longer has a host arm or a UI take to decide, and C3/C4 are decided on
    own-thread children only. S5's ui_lane becomes R0's child L3 Thread on the UI thread, not an executor pointer.

S4. The family tree and the mapping written only on the owner's lane (L2, L3), then read without the lock.
    Each tree cell and mapping cell has one writer by decision 18's ownership list (1)-(3), Mikhail's own
    clarification: the parent for its child chain and the mapping, the Message itself for its own cells. A cell with
    one writer and a reader on the same lane needs no lock. Cross-lane readers read only single-writer control flags,
    atomic cells whose order does not matter (Mikhail).
    Depends on 5.6 (a capability after its target closes) for C3/C4, and on N's per-query decision.
    - Red: 57's tripwire on every tree and mapping write, run over the executor selftest and model tests. Red where
      a write happens off the owner's lane today: endp_try_retire unlinking from any lane that drops the last
      reference, orphan_children's re-rooting, the host arms of mapping_authority.
    - Green: the tripwire green, then the lock calls around L2/L3 removed, the UAF kit clean.

S5. The runtime-level lists (L5). Waits on 5.1.
    rt->slots, rt->n, the retire queue, the reap queue, rt->transport and e->ui_lane each either go or become a cell in
    a Message's arena, by Mikhail's answer to 5.1. With "the executor frees its own memory", reap and join go: each
    worker frees its own wait state and ends. LmxMsgCtxPack, allocated on the parent's lane and freed by the child's
    thread (alloc inventory 2868/2993/3086), becomes the child's own first state.
    - Red: per list, a test that drops a Message's last reference on a lane other than its parent's, and checks with
      the tripwire that its storage is freed by its parent's settle write on the parent's lane.

S6. The lock deleted (L4, L7, L8 and what remains).
    - Red: lmx_msg_exec_lock and unlock defined to abort() in a test build. Every remaining call site is red by name
      in the executor selftest and model tests.
    - Green: grep -c lmx_msg_exec_lock at 0 in exec.c, lm1 and lm2, and the host lock gone. The only lock left is
      m->mail, the message collection's (lm1/lm2 mail_lock 6+6, exec.c mailbox functions). The gates and the UAF kit
      are green.

A1-A3. The arena (19.29.2), after S6 or interleaved where a stage touches the same record.
    Mikhail on 5.4, verbatim (2026-09-15): "какая копия? У нас нет никаких копий message -- выше же с тобой обсуждали.
    Есть только он сам и у него своя арена. Пускай это будет один alloc но это его собственный. При получении Message
    который не L3 Thread присоединяется к арене получателя. Всё". So A3 no longer waits: the mailbox holds Messages,
    each with its own arena, and a received Message that is not an L3 Thread joins the recipient's arena. The
    envelope copy made at admission (LmxMsgCopy: calloc in host_post, send, send_graph, send_owned, send_cap,
    post_dead, copy_dup, ui_request) is today's artefact and goes.
    A1. The Message's control record (LmxMsg, today calloc in slot_new 418) becomes a Structure in its own arena,
        created on the launching parent's lane; lmx_sched_record and lmx_root_record already follow that pattern.
    A2. The arena's bookkeeping (LmxOwnedRange and LmxMsgBlock records, calloc at 652/690/737/780/3633/3698) and the
        Message's own arrays (path, init, done_*, roots) are allocated from that arena.
    A3. An admitted copy's arena (5.4). Mailbox entries (LmxMsgCopy) follow his answer.
    - Red for each: the balance test's two-cycle live count (ld --wrap) with the record's allocation counted as
      heap. It is red while any such record comes from malloc, and green once the arena's blocks are the only heap
      allocations.

QUESTIONS (Mikhail's; the coordinator put 5.1 and 5.3-5.6 to him directly, so they are not repeated here)
- The design's own: for N, the 24 address queries from outside any Message (state, inbox_n, path_n/seg, init_copy,
  child_n/at, tracked, endp_refs, handoff_ready, native_users, adopted_*, exec_bind_*, is_bound, last_status,
  map_queued, test_*), does each query become a read on the owner's lane (the test runs it inside that Message's
  turn or its parent's), or is it deleted with the property it checked named? Proposed per query in S4's commit.
