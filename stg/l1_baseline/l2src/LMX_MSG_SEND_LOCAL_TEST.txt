L1 Message local-send regression
===============================

run_msg_send_local.ps1 builds tests/lmx_msg_send_local_selftest.lm1 with pinned
stable gen2 against a private git archive of core checkpoint
593a64baa2ca6f7e25094620dda4927243bd5c3c. Optional -CoreCommit selects another
explicitly agreed compatible revision, never the changing working tree. Seventeen
core source files are archived; all build outputs/logs/hashes stay in the unique
build/codex/msg_send_local run directory. No shared gate or app pin changes.
The snapshot includes the L1 path-storage helper integrated at 6880ea5; the
removed native path-growth definition is not silently used by this test.

Edit-loop default is now O2 only. Use -Optimization O0 for a debug build,
or -Optimization @('O0','O2') when both configurations are relevant. Repeated
and mixed-case entries are normalized/deduplicated; NULL/empty/unsupported
selections fail before work rather than producing a PASS with no tests.
The selection logic was checked on PowerShell5.1/7.6 without recompiling the
already verified runtime (build/codex/runtime_runner_options/options_ps*.json).
The O0/O2 results below remain historical evidence, not a mandatory double run.

136 checks at O0/O2, test object compiled with -Werror:
- send to self, direct child, parent, and a recipient in another branch;
- staged dest_msg is the exact recipient object, no early inbox admission;
- commit empties the outbox and admits exactly one binary envelope;
- normal send copies data: caller modification after staging cannot affect it;
- id/correlation/kind/number, embedded zero and high-byte payload values survive;
- an unrelated sibling's queues, init pointer and state remain unchanged;
- send_owned clears the sender envelope's bytes/n and preserves the allocation
  address through staging, admission and recv;
- receiver release frees that allocation once; repeated release is empty;
- init_copy on the owner fallback and inside a real bound Message turn:
  metadata, invalid address/null output, unchanged output on invalid input,
  independent returned allocation, mutation independence and repeated release;
- real turn_self context, state/inbox/path/child queries and restoration of
  the caller's empty turn context after the callback returns. Exact check count
  prevents an unexecuted callback from silently passing.
- two envelopes admitted BEFORE the recipient is bound: binding the UI turn
  followed by two bounded ui_step calls (no manual ready or direct callback
  invocation) consumes them in FIFO order exactly once, then returns EMPTY.
  This is the small deterministic L1 scheduler-boundary regression, not the
  native worker cleanup-gate test or a claim that the final scheduler is done.

The returned init buffer has a const API view; the test explicitly casts its
own allocated copy solely to probe independence, not the original init data.
Allocation-failure injection and concurrent races are not covered here.
Verified run: build/codex/msg_send_local/20260911_161340_490_6c7b6e74/evidence.json.
The snapshot now compiles/links lmx_msg_slots as required by593a64b exec.

Targeted follow-up for2b567e8 (unbound exec_ready exclusion): O0,136checks,
zero failures. Reused the already compiled private binary; no translator
rebuild, O2 rebuild, family suite, OOM suite or full gate was needed here.
The first Windows PowerShell5.1 run exposed a runner failure before test
execution: Start-Process rejects an explicitly empty ArgumentList. Both owned
send-local/family stage helpers now omit that option when there are no args.
Validated those exact helpers in PowerShell5.1 and7.6: no-argument launch,
argument-bearing launch and nonzero-exit rejection. This is runner validation,
not a new family/runtime result. Original failure evidence remains unchanged;
supplemental reports are runner_compat_ps5.json / runner_compat_ps7.json under
build/codex/msg_send_local/20260911_163600_204_276e870f/.

The fixture uses ONE root, with P's children C/S and a separate Q/T branch.
P->T exercises the prototype's fallback lookup outside P's subtree/parent.
The existing unsigned LmxMsgAddr and fallback lookup are prototype details, not
an endorsement of a global directory/capability model. This test deliberately
does not remove them or claim they are the final language architecture.

Only test logic is new, authored in L1. The free wrapper and its observation
variables belong to a single-thread test driver, not shared LMX runtime state.
No handwritten C test shim, private locks, threads or production hooks. Existing
native substrate is built from the archived checkpoint; it currently reports
one unused adopt_push warning under the established strict pointer guards.

This is delivery/ownership and self-turn behavior regression, NOT proof of
concurrent fairness, lock/table removal, current graph-owner lookup or full arena
handoff. Owned byte-envelope transfer and Message arena transfer are distinct;
the latter has separate storage and family-handoff regressions.
