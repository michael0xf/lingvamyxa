L2 open points ledger
=====================

Why this file exists
--------------------
Lingvamyxa_handoff_notes.txt section 4: consult the actual current source
sections for remaining decisions. They are not all to be answered in
advance - they stage. But an autonomous agent that meets one does not stop; it
invents an answer silently, and that is how architecture nobody accepted gets
built. So every unit of work must say which open points it touches and what it
does about each.

This file is that statement, kept per unit and mechanical. It is not a plan and
not a decision record: a point leaves this file only when the decision is
written into the spec, and then this file cites where.

Current decision check (2026-09-12)
----------------------------------
The source definitions close Structure len and typed void * children, auxiliary
source names, the shared merge/Message used-graph copy traversal, language-owned
payload copying, result/throw carrier and native arithmetic. L2 is a low-level
language; do not impose L3 ownership/lifetime rules on its ordinary addresses.
Also DECIDED: @ is ordinary address-taking, forbidden in L3; same-name bind
keeps the argument variable/address; @: char "hello" stores a raw char * in its
typed array, with no String/Array length descriptor. Field slots are fixed and
their void * references may change; checkpoint writes dirty values, with ordinary
type-change handling and assert on store failure. Lexical lookup may follow node
to zero. fn uses its existing C name and own-node, lexical, dynamic and explicit
inputs. Merge failure is throws merge(args). Existing graph retention/local
handoff is not blocked by remote codecs or budgets. See SPEC 11.2.1, 11.3.1,
19.17, 19.29, 21.3, 21.5, 21.6, 21.8, 21.13; ABI 6 and 14.19/14.21-14.23.
Latest user correction: merge copies the entire USED lexical tree/graph by the
SAME traversal as new Message creation, with explicit destination node fixups.
independent cuts outer lexical surroundings at zero. Methods and their immutable descriptors are shared; node copying retains the
same method references. No new user decision is needed about scalar sharing under the withdrawn pointer-only merge premise.
The current three-person implementation is defined in CORE_TEAM_PLAN_20260912.md.
Only a concrete logical contradiction in the model warrants a user question;
missing implementation or a stale document is not a new language decision.
Before opening ASK, read the current source section and the latest user
decision. When a decision changes documentation, update the source definition,
its acceptance criteria and every active ledger/handoff in the same checkpoint;
an appended override alone does not repair a contradictory requirement.

L1 import-storage correction (2026-09-12)
-----------------------------------------
DECIDED by the user: arbitrary sixteen-entry and 1040-byte storage-cell caps
are not language rules. A path is stored through the array/string descriptor
length/data, distinct from Structure.len. Codex implemented the existing
LmP0Text representation with growing tables in b41af667; 24 checks passed.
See L1_IMPORT_CAPACITY_20260912.md. Old independent path-buffer/depth limits
remain implementation work; do not present them as model decisions.

Rules
-----
R1. Before a unit is committed, every open point it touches gets a row here.
R2. A row says exactly one of:
      DECIDED   - the spec already answers it; cite section.
      AVOIDED   - the unit is written so the point does not arise; say how,
                  and name what would make it arise.
      ASK       - the unit cannot proceed without an answer. Stop and ask the
                  user (L2 semantics) or cursor (L1 capability). Do not guess.
R3. No row may say "assumed", "for now" or "probably". If the honest answer is
    a guess, it is ASK.
R4. Writing a value into a field whose meaning is open is not a decision only
    while nothing reads it. The moment a read appears, the row becomes ASK.

Sources
-------
  SPEC  Lingvamyxa_spec.txt
  ABI   struct_refactoring_version_2.txt section 14 (resolved and open entries)
  L1    L1_spec.txt section 6 (L1's own open points, they constrain the target)
  NOTES Lingvamyxa_handoff_notes.txt


Decision - Executable body hosting vs receiver arguments (2026-09-11)
-------------------------------------------------------------------
SPEC 21.5.1, 11.3.1; source implementation deferred until current Message done.
    DECIDED every statement-by-statement executable body is in the graph;
    arguments of every receiver, including trailers, acquire no automatic
    graph fields. Consumption role, not last-field syntax, distinguishes them.
    Same-name argument->own bind remains. Hosts are individual bodies with
    actual nesting; no new method/through boundary. All three translator
    passes must agree; current for-only hosting is an implementation gap.
    AVOIDED changing Message architecture, argument referent identity or
    checkpoint semantics. No emitter changes in this documentation checkpoint.

Unit - Integration regressions follow L1 path-helper integration (2026-09-11)
--------------------------------------------------------------------------
Files: run_msg_family_handoff.ps1, run_msg_send_local.ps1 and their docs.

SPEC 19.29.7.1
    DECIDED private test snapshot advances to 6880ea5, including path storage
    authored in L1. Existing public test assertions/semantics unchanged.
    AVOIDED shared working-tree builds, stable/app promotion or new API.

Unit - Private Message placement-path storage in L1 (2026-09-11)
-------------------------------------------------------------
Files: lmx_msg_path_storage.h.lm1 / .lm1, its test/runner and
       LMX_MSG_PATH_STORAGE.txt.

SPEC 19.29.7.1; LMX_MSG_CONTEXT_V0 placement-path representation
    DECIDED retain existing private CHUNK=4 doubling/overflow behaviour,
    status ABI 0/2/3 and failure-preserving realloc, now authored in L1.
    AVOIDED child-address assignment, root/owner ABI, registry/scheduling
    policy and graph/arena relocation. Only private path/capacity slots
    enter the helper. Grok retains Message call-site/build integration.

Unit - L1 local-send integration test (2026-09-11)
-----------------------------------------------
Files: tests/lmx_msg_send_local_selftest.lm1, run_msg_send_local.ps1,
       LMX_MSG_SEND_LOCAL_TEST.txt.

SPEC 19.29.7.1
    DECIDED explicit send is not limited to parent/child; staging, admission
    and execution are distinct. Test checks self/child/parent/other-branch sends,
    sibling isolation and explicit send_owned envelope-buffer identity.
    AVOIDED capability representation, global-table removal, native scheduling
    and arena transfer policy. Uses public APIs of a pinned private core
    snapshot; no production hook or application synchronization is added.

Unit - L1 family-handoff integration test (2026-09-11)
----------------------------------------------------
Files: tests/lmx_msg_family_handoff_selftest.lm1, run_msg_family_handoff.ps1.

SPEC 19.29.8 / 19.29.7.1
    DECIDED bottom-up failed arena retention, separate successful cleanup and
    unchanged native addresses. Exercises embedded owner-local range records,
    sibling isolation and detach-before-free through existing public APIs.
    AVOIDED root/current-owner lookup, capability ABI migration, scheduling and
    concurrency policy: test uses a private snapshot of the agreed prototype
    checkpoint, not a new implementation or proof of complete Message semantics.
    Native free instrumentation belongs to this single-thread test driver only.

Unit - Paired Message storage handoff (2026-09-11)
------------------------------------------------
Files: lmx_msg_storage.h.lm1/.lm1, dedicated tests/runner/doc;
       lmx_msg_blocks can_move extension.

SPEC 6.5, 19.29.6-7 / ABI 14.9
    DECIDED nonmoving ownership transfer includes owner-local range metadata.
    Both lists are preflighted before changing either. No allocator, graph copy,
    hidden state, new owner field, scheduler or lock is introduced.
    AVOIDED current-owner lookup from a method node, live reparenting, eligibility
    and native cleanup: the lifecycle caller supplies exclusive access to both
    already eligible owners. This is not a concurrent/distributed transaction.

Unit - Message-owned range metadata (2026-09-11)
-----------------------------------------------
Files: lmx_owned_ranges.h.lm1, lmx_owned_ranges.lm1, dedicated tests/runner/doc.

ABI14.9 / SPEC6.5, 19.29.6
    DECIDED by user: OS process startup is already the root Message. All state,
    including range metadata and immutable domains, is Message-local. NO shared
    LMX data, even immutable; only OS/non-LMX libraries are external.
    Range metadata follows arena ownership, without payload relocation. This
    resolves Unit1's per-owner subdivision ASK; its flat mutable global table
    remains prototype debt, not an accepted concurrent implementation.
    AVOIDED: lifecycle eligibility, external-library resource access, tracing
    and allocator policy. Storage API receives the owner's set explicitly;
    it does not create an external registry, global lock or scheduler.

Unit - Message-owned block storage (2026-09-11)
----------------------------------------------
Files: lmx_msg_blocks.h.lm1, lmx_msg_blocks.lm1,
       tests/lmx_msg_blocks_selftest.lm1, run_msg_blocks.ps1, LMX_MSG_BLOCKS.txt

ABI 14.9 / SPEC 2, 19.29  Nonmoving blocks, transfer and arena lifecycle
    DECIDED storage-only split: LMX_MSG_ARENA_SPLIT.txt records Grok/Codex
    ownership and the three operations. Transfer preserves native pointers and
    clears the former owner's list. No copying, scheduler, flags or global lock.
    AVOIDED eligibility policy: caller must establish authority, quiescence and
    child settlement; the module does not infer safety from running/success.

ABI 14.2-14.3  Lmx len interpretation and empty language value
    AVOIDED. Metadata n counts allocation bytes, not Lmx.len. Tests use raw
    Lmx pointer links without reading len. Empty block list is storage ownership
    bookkeeping, not a representation of an empty language Structure.

ABI 14.8-14.9  Registered range ownership and full graph integration
    AVOIDED. class is opaque allocation metadata carried unchanged, not a
    classifying registry or a tag on a source node. Full range/failure-root
    integration remains separate work; arbitrary escaped aliases are not proven
    absent by list validation. No shared mutable classification table is added.

L1 1.1 / 10.16  Header units and typed disposer callback
    DECIDED .h.lm1 / fnptr spelling; verified header emission and separately
    linked consumer in O0/O2 tests. No hand-written C wrapper or semantic shim.


Unit 1 - Lmx core and classification by registered address range
----------------------------------------------------------------
Files: lmx.h, lmx_range.lm1, lmx_selftest.lm1, run_lmx.ps1

ABI 14.2  Branch len
    DECIDED: immediate child count (ABI 14.1/14.2, SPEC 2). Array entry length
    is separate. lmx_cell_init originally only zeroed storage; that historical
    implementation scope does not leave branch len semantics unanswered.

ABI 14.3  Empty representation
    AVOIDED in this unit: it did not distinguish null data from an empty
    Structure. Any concrete remaining empty-value encoding issue is separate
    from the settled branch child count.

ABI 14.8  Arrays: pool block allocation, growth, range registration
    Range registry table: AVOIDED for growth. lmx_ranges_init takes a fixed
    capacity and lmx_range_register refuses to grow past it, so no registry
    growth policy is invented. Registration itself is half-open [lo, hi)
    membership with a stride (the minimum 6.5 already states).

    Pool blocks: DECIDED separately as non-moving chunks with per-block
    registration (SPEC 2 / 6.5; ABI 4.1 / 7.2 / 14.8). Existing addresses
    stay unchanged when a new chunk is added. Registry capacity is not
    that pool-growth rule.

ABI 14.9  Allocation, arenas: physical subdivision into registered ranges
    AVOIDED. The table is one flat global list of ranges. SPEC 6.5 grants that
    range classification is globally available and that this shares no mutable
    cell between Messages, so a global table of BOUNDS is in scope. What is not
    settled is whether ranges are physically subdivided per arena. Arises the
    moment a Message arena exists and wants its own subdivision. Then the flat
    table is wrong and this becomes ASK.

ABI 14.1  Child storage
    DECIDED: void * child array, typed by each target address; len is only child
    count. Unit 1 did not implement this layout. See SPEC 2 and ABI 3.3/14.1.

L1 10.8   One C TU vs many; init-before-main
    AVOIDED. The selftest composes with predef:, so everything stays one
    translation unit. Arises when service pools must be initialised
    program-wide across modules. Then: ASK work_chat/grok (L1 capability).

L1 10.9   Field image \ vs . vs ->
    Not an open point, and not one this code may reopen. The user has ruled
    Valued member access out everywhere, L1 and L2 alike: if something is
    written with it, it gets fixed. The single admitted route is the explicit
    C door - c.value.field, which lowers to value.field and which l1trans
    already supports today - reserved for extravagant exceptions.

    So \ lowering unconditionally to -> is correct, and address forms here are
    permanent, not a stage waiting on an L1 capability. SPEC 11.2 (around line
    7133) says the same for L2 on its own terms: minimal field-follow is
    address-oriented, value\field is the direct form when the binding is a
    typed address. Taking a pool entry by value would discard the very address
    that classification reads, so the rule and the mechanism agree.

    History, kept because the mistake is instructive. This unit asked for the
    dot and was wrong twice over: it was writing C-shaped records where the
    spec keeps pool entries addressed, and it read an L1 capability question as
    an L2 semantics question. The request was withdrawn in
    work_chat\cursor\inbox\20260908-004726.txt. A grok_bot task to implement
    the dot in L1 was raised off the back of it and had no consumer at all -
    l1src itself never takes a member by value anywhere, and self-hosts to a
    gen1=gen2=gen3 fixed point without it. The user cancelled it directly.

SPEC 11.3.1  `@` of graph payload, declared parameter, hidden through
    DECIDED (docs 20260908-111800, corrected 20260908-115700). After range
    classification, `@x` of an own graph field is the stored payload, not
    `&Lmx.data` and not the C cache. `int` is the example, not the only
    kind. Declared and through parameters are ordinary C parameters: `@x`
    is `&x`, no graph-materialization, no writeback. L1 `p: @ value` remains
    C `&value` (L1_spec.txt) and matches L2 parameter `@`, not L2 own-field `@`.


Unit 2 - typed service pools
----------------------------
Files: lmx.h (LmxPool), lmx_pool.lm1, lmx_pool_selftest.lm1

SPEC 6.5  Array descriptor len
    DECIDED, and by the spec, not by this code. 6.5 says the descriptor has
    exactly two values, length and a pointer to element storage. So
    LmxArrayDesc.len is element count and may be read. This is a DIFFERENT
    field from the len in the Lmx header, which stays unread - SPEC 2 says so
    outright ("Array-descriptor len is a different field").

ABI 14.8  Arrays: pool block allocation, growth, range registration
    DECIDED. lmx_pool_open takes a chunk capacity; lmx_pool_take adds chunks
    when needed, each with its own registered range and the same kind/type.
    Existing addresses stay unchanged. The original fixed-capacity-only unit
    has been superseded; see the chunking correction below and SPEC 2 / 6.5,
    ABI 4.1 / 7.2 / 14.8. Chunk-sizing policy remains open.

ABI 14.4/14.5  Auxiliary source names
    DECIDED (ABI 5.1, 14.4/14.5; SPEC 2, 21.1): address -> short source
    Structure name is reference information for strings. It is not a descriptor
    or execution ID. No uniqueness, collision policy or mandatory named,
    anonymous or positional registration is required.
    Exact callable sig is a separate method contract (SPEC 21.8); this table
    neither encodes it nor blocks method dispatch.

ABI 14.9  Allocation and arenas
    AVOIDED, and worth restating now that memory is actually allocated. Pools
    call calloc directly because no Message exists yet. 14.9 fixes that every
    Message owns exactly one logical arena and that all dynamic mutable
    ordinary Lmx storage of that Message is allocated there. Arises at the first
    Message. Then pool blocks must come from its arena, and the flat global
    range table may need per-arena subdivision. Then: ASK.

ABI 14.10 / SPEC 21.13 p.4  Method result and failure carrier
    DECIDED 2026-09-12: status return plus typed ordinary-result and throw
    out-parameters. Declared throw and runtime failure have distinct statuses.
    A throwing call does not store its ordinary result. Storage belongs to the
    activation/Message; no process-static throw channel. longjmp is for
    diagnostic assert only. This original unit stored addr without calling it;
    invocation work implements the settled ABI instead of asking it again.


Unit 3 - the interned character pool
------------------------------------
Files: lmx_chars.lm1, lmx_chars_selftest.lm1

ABI 14.7  Primitive copy and mutation policies
    Half DECIDED, half AVOIDED, and the split is 14.7's own. Its first sentence
    settles this unit outright: "Interned immutable leaves naturally share their
    primitive address." That is implemented and asserted - the same character is
    the same cell, always. What 14.7 leaves open is everything else: mutable
    cells, large numbers, byte buffers, opaque handles, foreign resources, each
    to be specified by physical range. None of those is offered here. Arises at
    the first mutable primitive cell, which 14.9 also requires to live in the
    owning Message's arena. Then: ASK.

SPEC 21.4  Primitives pass by semantic value
    DECIDED, and demonstrated rather than assumed. 21.4 says the callee gets an
    independent value "even when the source Lmx primitive is physically
    represented by a pointer into a registered address range such as
    all_chars_array", and that sharing an immutable atom is an implementation
    detail. The selftest carries 2's worked example for it: two fields holding
    one character hold one address, and reassigning one changes only that
    field's reference while the shared cell and the other field stay put.

    Not an open point, recorded because it is the claim most easily broken by a
    later optimisation. If a field write ever reaches the cell instead of the
    reference, this test fails.

    Limitation stated rather than hidden: immutability is enforced by
    construction - the block is filled once at init and no mutator exists - not
    by the C type. Saying it in the type belongs with the per-range physical
    class policy that 14.7 leaves open.


Unit 4 - WITHDRAWN. There is no name table.
-------------------------------------------
A reverse name index was built and then removed, because the model behind it
was wrong. Kept as a row rather than deleted, so the error is not made twice.

Field names are known at translation time. treeranch resolves to a fixed
index in the branch block; the runtime does pointer arithmetic and the
generated C never sees a name. SPEC 2 says it plainly - "A resolved reference
is used directly and needs no name lookup" - and the type of a void * comes
from the address range it falls in, never from a lookup. The plan this unit was
built for, "enumerate children, ask the index for each short name, compare",
was runtime work for a question already answered at translation time.

SPEC 2 does keep one name aid on the books: the reverse index
Lmx * -> ShortNameId, for runtime TEXTUAL lookup and for reconstructing a
current full path from sibling ordinals and node links. That is diagnostics and
genuinely dynamic access, not ordinary field resolution. If it returns, it
returns for those two things, and its absence until then is what keeps the
compiled path honest.

ABI 14.4 therefore blocks nothing here. It was recorded as blocking unit 6;
that entry was wrong for the same reason.

Constraint carried into unit 5 - occurrence ordinals are naming, not indexing
-----------------------------------------------------------------------------
Recorded because getting it wrong would have shaped unit 4's id and could not
have been undone quietly.

Two brackets that look alike are grammatically different (SPEC 12.3, and 21
states it outright: the bracket in target\[occurrence]field is not the postfix
operator).

    field\[2]field    the third occurrence among children whose SHORT NAME is
                      field. The leading bracket is part of the NAME. Bare
                      field is shorthand for [0]field (SPEC 2).

    field\field[2]    element 2 of the array held by [0]field. The trailing
                      bracket is postfix indexing of the VALUE (SPEC 12.3),
                      which explicitly does not select the nth repeated field.

Consequence for unit 4, which it already satisfies: the ordinal is POSITIONAL.
It comes from the order children are enumerated in, and is not stored. The
auxiliary table may record the same short source name for several addresses;
source occurrence selection does not require a unique name ID or full-path key.

Consequence for unit 5, as corrected by the user. In LMX code the field is
simply branch, and priority belongs to the EARLIER occurrence. The [N] form is
a translator-side extension of the name, used to address later duplicates; it
is not how ordinary source spells a field.

Withdrawn unit-5 proposal, corrected in place 2026-09-12:
The old proposal required runtime name probes, canonical IDs and collision
decisions. It was incorrect. The translator resolves source fields to child
positions; compiled execution follows addresses/indices without the auxiliary
name table. Branch layout and len are resolved in ABI 14.1/14.2. Source names
are only address -> short-name reference information (ABI 14.4/14.5), including
for strings which do not correspond to a tree. No name ABI blocks unit 5.


Unit 5 - reference-value passing
--------------------------------
Files: lmx_ref.lm1, lmx_ref_selftest.lm1

SPEC 21.4  Structure and Array arguments are reference values
    DECIDED, and pinned rather than assumed. 21.4 fixes both halves: the
    reference is copied into the callee's activation and the referent is not
    cloned; rebinding the callee's local reference changes only that local
    reference; a write through the reference mutates the shared referent and is
    visible through every reference and path to it.

    This unit has no open points. It exists because both halves are exactly
    what a later change would break quietly - by cloning on the way in, or by
    passing a reference to the reference so a callee's rebinding escapes. Either
    would still pass every other test in this tree. The helpers therefore do the
    tempting wrong thing locally, rebind and reassign, so the selftest can show
    it does not get out.

    The array case covers the distinction 21.4 draws for Array operations:
    replacing a descriptor's own storage mutates the referent and every holder
    sees it, while assigning a local reference to another Array does not rebind
    the caller. Both are asserted against the same descriptor.

    Not covered, and not by omission: 21.4's last paragraph forbids passing a
    raw Structure or Array pointer as is between Messages, requiring a clone,
    serialization or ownership-transfer boundary. No Message exists yet. Arises
    with the first one, alongside 14.9.


Audit: what may change in a Structure, and what it gives 14.1
--------------------------------------------------------------
Asked to check whether the rule had drifted. It has not, but the first reading
of this audit drew the wrong consequence and is corrected here.

The rule, as the user states it and as the spec bears out: THE NUMBER OF
CHILDREN DOES NOT CHANGE. What may change is the REFERENCE a field holds, and
only where that field is not const - 9.1.1's const fixes the binding, the cell
or reference, and does not reach through it. Unit 3 already carries the worked
example: assigning a primitive field changes its reference and leaves the
shared cell alone.

Every site that mentions appending is consistent with that once merge is read
correctly:

  19.20   construction and merge are distinct operations; merge CREATES A FRESH
          ROOT and copies operand children into it. It does not grow an
          existing structure.
  ~11089  merge appends copied children in operand/body order - into that fresh
          root
  19.29.12 merge copies the complete used graph and lexical tree to node = 0
           by the SAME traversal as Message creation, explicitly rewriting
           destination node and child/payload references (2.3).
           independent cuts external lexical fallback, not typed dynamic
           inputs or expression-selected calls.
  21.11   live field count/slots are fixed; void * child references may change.
          Operations needing another field count construct another Structure.
  ~6063   an ordinary assignment neither updates node\[0]i nor appends a child
  ~7684   an explicit append never moves the existing first child from [0]

Corroboration I should have noticed before extrapolating: 14.1's own
requirement list is "stable node addresses, ordered children, deep containment
copying and range classification". GROWTH IS NOT IN IT. Had branches been able
to grow in place, 14.1 would have had to say so.

Current 14.1 resolution: child storage is an ordered array of void * pointers,
with len equal only to child count. Classify each stored target address by its
typed array/ranges. Merge and Message creation copy the complete used graph and
lexical tree to zero with the SAME traversal; destination node and references
are explicitly remapped. independent cuts external lexics. The old shallow
merge/no-ancestor-copy premise is withdrawn (SPEC 2.3, ABI 8.2, 14.6).

Method note to self. Twice now a correct local reading produced a wrong
consequence, because I extrapolated from the passage instead of checking it
against the requirement list that governs it. The list is the cheaper check and
comes first.


Correction: N typed service arrays means one per TYPE, not per category
-----------------------------------------------------------------------
Units 1 to 3 registered a range under a KIND - primitive, method, array - which
is the column SPEC 2's table gives for how to read an entry. That is only half
of it. 2 says the implementation has N TYPED service arrays, and 6.5 spells one
of them out: "For every used Array type T the implementation provides a typed
service pool, schematically all_array_of_T."

So a range now carries a concrete type as well, and the address gives that type
outright. A descriptor from all_array_of_int and one from all_array_of_char are
both array descriptors, read alike, and are told apart by which range they land
in - with nothing stored in either to say so. The pool selftest asserts exactly
that pair.

Kind and type are both kept because they answer different questions: kind is how
to read the entry, type is which typed service array it came from.

Not an open point - a misreading, corrected.

Carried into the const rule
---------------------------
The user's rule, recorded before the unit that will need it: if a reference is
const, everything is done WITHOUT touching the node the void * lives in. Not
"the resolution may be cached" - the generated C does not load node->data at
all, because the target is known at translation time. That is 21.8's direct
invocation for callables, and the same for ordinary const fields.

This is the sharp end of NOTES section 3.1, which insists devirtualisation and
checkpoint elision are separate optimisations with separate preconditions. const
gives the first outright. It does not give the second: whether the caller has
dirty own fields across the call is a property of the CALLER, and const on the
callee says nothing about it.


Correction: a typed service array grows, and grows by chunking
---------------------------------------------------------------
Unit 2 refused to grow and recorded that as AVOIDED. That was wrong: a
non-const variable LIVES in the big array of its C type, so the array has to
grow. Corrected, and the growth mechanism is forced rather than chosen.

DECIDED, confirmed by the user on 2026-09-08 and reflected in SPEC 2 / 19.29
and ABI 4.1 / 7.2 / 14.8–14.9: live nodes and service-pool entries never relocate.
The older permission to move after patching all references was a documentation
error, not a supported alternative. Growth needs no reverse-reference inventory
or pointer-patching pass. Allocation failure may terminate the owning Message
and release its arena before a fresh Message is started.

Growth therefore adds a CHUNK, and each chunk registers as one more range with
the same kind and type. A typed service array is a SET of registered ranges, not
one range. Every address handed out before a growth stays valid and still
classifies, which the pool selftest asserts directly: after growth the earlier
entries keep their type and their contents.

Two disciplines share this machinery, and the contrast is the point:

    interned immutable atoms   all_chars_array has its 256 cells from init,
                               addressed by value, never grows (14.7 settled)
    mutable variable cells     handed out one at a time, the array grows to fit

What makes an entry a char is the range it sits in, not which discipline put it
there.

ABI 14.8  Pool block allocation, growth, range registration
    DECIDED in SPEC 2 / 6.5 and ABI 4.1 / 7.2 / 14.8:
    growth is chunking with per-chunk range registration. Still
    open: how big a chunk should be, and whether chunk size should vary. The
    code takes a chunk capacity at open and keeps it, which is a placeholder,
    not an answer.

ABI 14.9  Allocation and arenas
    Unchanged and still ASK at the first Message. Chunks come from calloc
    because no Message exists yet; 14.9 requires them to come from the owning
    Message's one arena.

Cost to watch, recorded now rather than discovered later. Classification scans
ranges linearly, and chunking multiplies the number of ranges. That is fine
while ranges are few and it is on the hot path, so it will need a real lookup
before it is fine at scale. Not a correctness problem, and not solved by
guessing at this stage.


Standing contract: the two optimisations, kept verbatim
--------------------------------------------------------
Not open points. This is the performance model of 21 and the part most easily
lost by a later change that looks locally reasonable. Recorded so that a unit
which generates calls can be checked against it line by line.

A. const, and the internal consequence of immutable

  const fixes the BINDING - the cell, the reference - and is shallow; it does
  not reach through the reference. immutable fixes the VALUE and is deep. They
  are two orthogonal axes, neither a stronger form of the other (9.1.1, 9.1.2).

  If a reference is const, the generated C does not touch the node the void *
  lives in at all: the target is known at translation time. For callables that
  is 21.8's direct invocation - a direct C symbol call, with no walk through the
  descriptor via the node.

  21.5 adds the part that survives suspension: const qualification of an own
  field's backing, INCLUDING a RuntimeImmutable that succeeds while this
  activation is suspended in a call, stays binding on the cache. Neither a later
  own-local assignment nor a checkpoint may bypass it.

  What const does NOT give: checkpoint elision. Whether the CALLER has dirty own
  fields across a call is a property of the caller, and const on the callee says
  nothing about it. NOTES 3.1 insists these are two optimisations with separate
  preconditions and that they fail independently.

B. Own fields as C working locals, with dirty-only checkpoints

  Entry.     Load only OwnUsed(M) - the own fields this body actually reads or
             writes unqualified, or uses to supply an outgoing callee's dynamic
             value - into typed automatic C locals. Each local records and roots
             the exact Lmx occurrence it came from. Other node fields are
             neither loaded nor stored.

             Explicit node\field, reference\field and array[index] are NOT
             folded into this cache. They read or mutate their selected location
             on every execution. Mentioning a field through an explicit path
             does not make it a cached own local.

  Body.      Work in the locals.

  Dirty.     A local becomes dirty only when this activation actually executes
             an assignment or rebind to it since its last successful checkpoint.
             Not inferred by comparing against the graph. Not set because a
             branch might have assigned. Not set by capability: making writable
             access available is not a write, and an outlined helper must report
             the write it actually executed.

  Publish.   Immediately before every outbound call, before every external,
             re-entrant or publishing boundary, and before every exit -
             fallthrough, return, throw, failing assert, stop, cancellation, and
             before yield suspension - write each DIRTY local to its node field,
             in forward source order of the backing fields. A successful store
             clears only its own dirty state. Clean values must not be
             re-published. Declared arguments and dynamic inputs are never
             published automatically.

  Return.    NO RELOAD. The caller continues with its C snapshot; only explicit
             graph paths see the current graph.

  Why no reload, and why not flush-all. 21.6 gives the case outright: outer
  enters with x = 1 clean, calls inner, inner explicitly writes the graph field
  x = 2, outer returns without assigning its local. Flush-all would overwrite 2
  with a stale 1. Dirty-only writes nothing. If outer then executes x = 3, x is
  dirty and the next checkpoint publishes 3.

  NOTES 1.1 derives this from the ontology rather than postulating it: all
  Structure, one sequential thread per Message, a small C stack, and activation
  state living in invoking_structure rather than in a frame slot. A bare name LOOKS
  like an Algol-60 local and LIVES as an instance field; spill/no-reload is the
  seam between those two natures. Thread safety comes from the same single
  sequential thread - a suspended caller's snapshot cannot be raced, and other
  Messages have disjoint arenas.

C. The related case: values from the enclosing scope

  DynRequired(M) is a separate set from OwnUsed(M). Declared arguments and
  dynamic imported values are activation-local inputs with NO write-back target.
  Assigning or rebinding one changes only this activation and the value it may
  pass further down; it never writes back to the caller and never automatically
  writes node\x. To publish one as method state the program must assign an own
  field or use an explicit graph path.

  A forwarded dynamic name never becomes an own graph field merely by being
  forwarded.


Unit 6 - branch storage
-----------------------
Files: lmx_branch.lm1, lmx_branch_selftest.lm1

Current correction 2026-09-12: this unit's inline-Lmx child implementation and
tests are historical implementation evidence, not conformance to the user's
void * child-array contract. The core owner must audit/update branch allocation,
access, assignment, primitive storage, merge and tracing. Codex does not alter
the implementation in this documentation checkpoint.

ABI 14.1  Child storage
    DECIDED: Structure.data addresses an ordered array of void * child pointers.
    Each child's type follows its target address in the array/ranges of type T.
    Pointer-slot addresses do not identify referent types. No inline-Lmx child
    block or mandatory primitive wrapper. See ABI 3.3, 14.1 and SPEC 2.

ABI 14.2  len
    DECIDED: Structure.len is ONLY child count. Primitive children have no
    independent Lmx len; an Array record has its own separate length field.

ABI 14.6  Merge and Message creation: shared graph-copy traversal
    DECIDED: copy the complete USED graph and lexical tree to node = 0 with
    the SAME traversal. Explicitly rewrite destination node/child/payload
    references through the copy map, preserving aliases/cycles. independent
    supplies zero; result node identifies the containing Structure of merge.
    The question premised on A.x/R.x sharing a mutable cell after shallow
    merge is withdrawn: used cells are copied with the graph.


ABI 14.3  Empty representation
    Still open, and now visibly so. lmx_branch_open refuses a zero child count
    rather than deciding what an empty branch looks like. Arises when an empty
    Structure must be told from a null payload. Then: ASK.


Unit 7 - own fields as C working locals, with dirty-only checkpoints
---------------------------------------------------------------------
Files: lmx_own.h, lmx_own.lm1, lmx_own_selftest.lm1

SPEC 21.5, 21.6  DECIDED, and pinned before anything generates it.
    This is the machinery a generated method body carries, written by hand so
    the contract is testable now. It has no open points; it is here because
    both failure modes are SILENT. A flush-all checkpoint and an automatic
    reload each keep every other test in this tree green while destroying the
    semantics, so they get a test that fails on them specifically.

    The selftest carries 21.6's worked case verbatim rather than paraphrasing
    it: outer enters with the field at 1 and its local clean, calls inner,
    inner writes the graph field to 2 through an explicit path, outer returns
    without assigning its local. A flush-all implementation overwrites 2 with
    a stale 1. Asserted directly.

    Also asserted, each ruling out a plausible shortcut:
      a freshly loaded local is clean
      a clean local publishes nothing however many checkpoints it passes
      reading the local after a call returns the entry snapshot, not the graph
      an executed write sets dirty, and only an executed write does
      a dirty local does not reach the graph until a checkpoint
      a successful store clears dirty
      the local is aimed at the exact occurrence it was loaded from
      a sibling field, not being OwnUsed, is neither loaded nor stored

    What is deliberately absent. No helper turns an explicit path into a cached
    local - 21.5 says node\field, reference\field and array[index] read or
    mutate their location on every execution and are not folded into the cache.
    Declared arguments and dynamic inputs have no write-back target and do not
    appear at all.

    Current failure decision: checkpoint-store failure uses assert (SPEC 21.6,
    ABI 14.23). The old helper evidence did not exercise a failing store; that
    is test coverage, not an unanswered policy question.

    Current field decision: field count/slot positions are fixed; child void *
    references may change. Dirty checkpoint writes the corresponding field.
    A type change uses ordinary field-update handling. The old active-occurrence
    deletion/move/conflict question was based on an incorrect representation.


Unit 8 - L2 bootstrap main adapter (first vertical L2 -> L1 -> exe)
-------------------------------------------------------------------
Files: l2trans.lm1, run_l2trans.ps1, tests/entry_*.lm2

SPEC 1.7  Default L2 executable entry is `main`, bootstrap adapter not the
    universal source-method ABI.
    DECIDED. This unit emits L1 `external: fn: main () int / return: N`
    from a supported L2 `fn: main () int / return: N`. No `invoking_structure`,
    no sig, no hidden dynamic values, no Message.

SPEC 0.1.1  `.lm2` is an implicit L2 body; explicit `L2:` remains available.
    DECIDED. entry_return0.lm2 uses the wrapper; entry_return7.lm2 is
    wrapperless.

ABI 14.10 / SPEC 21.8 / SPEC 21.13 p.4  Method result, typed invocation, throw
    DECIDED ABI: status plus typed result/throw out-parameters, with distinct
    declared-throw and runtime-failure statuses and Message/activation storage.
    This bootstrap unit emitted only non-negative C int literal returns;
    it did not implement general method invocation or throw. That is an
    implementation boundary, not a pending carrier decision.

SPEC 21.8  sig
    AVOIDED in this unit: no method record is emitted. Exact method signatures
    are separate from the auxiliary source-name table.

ABI 14.9 / SPEC 19.28–19.29  Message arena
    AVOIDED. Generated exe does not construct Lmx, pools or a Message.
    Units 1–7 are not linked. Arises when a fixture needs Lmx. Then: ASK.

SPEC 11.3.1  `@` of graph payload / parameter
    DECIDED. Semantics are settled in SPEC 11.3.1. This adapter's
    implemented subset currently rejects `@` as unsupported body.
    A later fixture that uses `@` follows 11.3.1. ASK only if that
    fixture needs a contract 11.3.1 does not settle. An implementation
    gap is not a new user question.

SPEC 14 / 21.5–21.6  own-field cache, dirty checkpoints
    AVOIDED. No own fields, no unqualified locals besides the adapter.

SPEC 1.7 / 6.6.6  `c.puts` of a quoted string before `return`
    DECIDED for this adapter. Zero or more `c.puts` with one ordinary
    double-quoted atom; P0 spelling is copied into L1. Not a general
    `c.` call ABI and not a C: text hatch.

Unsupported forms (extra statements, bad/extra puts args, code after
return, several main, L1/L3 wrappers, non-decimal return) are
diagnostics, not silent L1 copy and not a canned output.


Unit 9 - L2 two-int methods in one unit
---------------------------------------
Files: l2trans.lm1, tests/add.lm2, tests/entry_sum.lm2,
tests/entry_add_ret.lm2, tests/entry_plus.lm2,
tests/entry_swap_formals.lm2, tests/unit_*.lm2,
negatives unknown/arity/unresolved/trailer/dup/loop/recursion

SPEC 21.8 / ABI 6.6  method record {addr, sig}, node first, typed invoke
    DECIDED for throws-none methods in this slice. C entry is
    `T l2_m{i}(Lmx *node, ...)` with backend symbols mangled by
    unit index; intern and find_method keep source names, so a
    source method named malloc does not collide with c.malloc.
    Two-pass: collect declarations+contracts, then validate
    bodies. Intern table is one per translated unit (not reset per
    method). Full contract = L2_SIG_V0 + exact ordered source
    formal-name bytes and types; method name is not interned.
    Matching contracts share an id. rec.sig is that interned id.
    Runtime does not memcmp. Child index is the callable path.
    Closed-unit devirtualization: no graph mutations after
    construction; actuals materialized left-to-right into temps
    before each call (not C argument order). Recursion and
    cycles are an implementation limit (`unsupported recursion`), not
    a language ban. Max 8 methods (9th is `too many methods`).
    Generated `lmx_ranges_init` is 1+N, or 2+N when OwnUsed char
    fields need the interned all_chars pool.
    Source `node` is reserved. Formals emit as `l2_p{i}_{j}`; intern
    keeps source names. Long names / too-deep if are diagnosed before
    dest is opened. Publication: write tmp; if dest exists rename
    dest to dest.bak, or dest.bak.N if dest.bak already exists, so a
    foreign regular bak is kept; rename tmp to dest; on fail restore
    bak; on restore failure report the backup path and do not claim
    dest was restored. Not atomic. No recursive FS. Files not created
    by this run are not deleted.
    Variable arity 1–4; char, int, size_t, or const @(char) formals;
    int or size_t result. Pure &&/|| (no calls) stay one L1 C
    expression with C precedence. Expressions that contain calls
    use guarded if/temps: || splits then &&; parenthesized
    STRUCTURE is a subexpression. The value of &&/|| is int 0 or 1
    (`(x) != 0`), not a raw operand, matching C even when a call
    is added to a previously pure expression. Non-logical size_t
    calls keep a size_t temp; they are not copied into int.
    Actuals are prepared left-to-right once; checkpoint runs after
    those actuals and immediately before the executed call. A
    skipped &&/|| RHS does not prep nested actuals or checkpoints.
    Partial ports: parser_text_predicates.lm2,
    parser_text_line_break.lm2 (lm_p0_line_break_width_at),
    parser_text_starts_python.lm2 (lm_p0_starts_python_string).
    They do not replace l1src. const @(char) is a declared typed
    C-address parameter (SPEC 11.3.1), not an LMX Array descriptor.

SPEC 21.5 / 21.6  own char field of invoking_structure
    DECIDED from the spec, not from C. `char: quote` in a method
    body is an OwnUsed graph-backed field of the containing
    Structure (`invoking_structure`), sibling of the METHOD leaves.
    Historical builder used an Lmx wrapper with data = all_chars cell.
    Current SPEC 2 instead stores that char-cell address directly in the void *
    child slot. This old implementation evidence does not establish conformance.
    Unqualified `quote` is the typed char C cache loaded at entry
    from that exact occurrence. Assignment sets dirty even when the
    value equals the cache. Checkpoint before outbound L2 calls and
    every return publishes only dirty caches by rebinding
    the historical wrapper's field->data to lmx_char_cell(cache); it does not mutate the
    shared char payload (SPEC 2 / 21.4). Clean never republishes
    (21.6 inner explicit write). No reload after a call. Explicit
    `node\quote` (P0 frame head `node\quote`) writes the graph
    child and does not touch the caller's cache. DynRequired stays
    empty in this slice. No per-activation graph objects. Throws /
    Message / yield / recursion remain implementation limits.

    DynRequired remains empty (no through names in this slice).
    OwnUsed is non-empty for methods that declare `char: name`.
    Not Throws/Message/through. prototype: is emitted so forward
    calls do not depend on C declaration order.

SPEC 11.3.1  declared typed C-address parameter
    DECIDED for this port: `const: @(char source)` is the C
    `const char *` formal of lm_p0_line_break_width_at. No L2
    protection, cloning, or Array descriptor is added. Null source
    with length 0 is the original guard (index >= length) and must
    not dereference.

SPEC 21.13.4 / ABI 14.10  result carrier
    DECIDED as backend choice for this slice: int or size_t return,
    throws-none.

SPEC 21.13.5  record layout
    DECIDED as backend: one malloc'd LmxMethod per method in a
    registered METHOD range; unit opened with that child count.

SPEC 21.5–21.6  OwnUsed/DynRequired
    OwnUsed: see the DECIDED row above for char fields.
    DynRequired remains AVOIDED as empty (no through names).
    node is the unit Structure.

ABI 14.9  Message arena
    AVOIDED. calloc/malloc like units 1–7. ASK at first Message.

SPEC 1.7  main
    Still the executable adapter only. Methods are not given that
    exception. Puts-only main stays the no-method path.


Unit 10 - parser_text views (text_equals, identifier_payload)
------------------------------------------------------------
Files: l2trans.lm1, lmx.h, lmx_size.lm1, lmx_size_selftest.lm1,
l2_text_hash.lm1, parser_text_views.lm2, run_lmx.ps1,
run_l2trans.ps1, tests/unit_unknown_*.lm2, tests/unit_const_write.lm2

SPEC 21.5 / 21.6  own size_t field of invoking_structure
    DECIDED same as own char: `size_t: value_length` is OwnUsed of
    invoking_structure, typed size_t cache, unique LMX_TYPE_SIZE_T cell
    (growable primitive pool, not interned shared size_t, not int).
    Dirty-only store through that cell; no reload. ASK B (activation
    scratch) is not implemented.

SPEC 11.3.1  declared typed C-address parameter
    DECIDED for this port: `const @(LmP0Text)` and `@: LmP0Text` are
    the existing C ABI in generated l1src/p0.lm1.h. data/length are typed raw
    projections (`->` in C, `\` in L1), not Lmx child indices.
    Include of p0.lm1.h plus this closed schema is a limited migration
    adapter, not a general C header parser. New L2 Structures do not
    gain a silent C layout.

SPEC 7487–7496 / variant 2  raw vs Lmx follow
    DECIDED for this adapter only: lmxParent\child stays child index;
    rawParent\child is -> for the declared foreign typed address.

c.strlen / c.memcmp / l2_immut_query_fill / l2_hash_compare_q
    DECIDED as known C-surface functions with exact signatures.
    fill and compare_q are temporary L1 adapters behind real
    §21.8 L2 methods make and equals_query. Unknown c.* is
    `unknown method`. const pointee forbids stores (`const write`).
    Out-pointer stores address the referent fields.

L2ImmutQuery borrowed adapter (ticket 171600)
    DECIDED as an explicit l2src C header, not p0.h and not an
    Lmx Structure with a silent C layout. Constructor contract:
    backing stays immutable and alive; a program-lifetime literal
    satisfies it; const formals do not prove that for an arbitrary
    buffer. make computes FNV-1a 64 once. equals_query hashes
    current foreign text bytes against that prepared hash, then
    memcmp. live==0 after failed/incomplete fill; compare does
    not read data/hash. text_equals remains strlen+memcmp fallback.
    No pointer cache. All-L2 debt: fill/compare_q stay L1 helpers
    until L2 has u64 stores; the methods themselves are L2.

Cluster B (copy_bytes / text_view_new_cstr / delete / from_cstr)
    Implemented as Unit 11. P0-as-Lmx and parser.lm1 scanners
    remain not touched. Frozen p0.h / bootstrap / root L1 unchanged.

Unit 11 - parser_text heap views (copy_bytes, new_cstr, delete, from_cstr)
-------------------------------------------------------------------------
Files: l2trans.lm1, parser_text_heap.lm2, run_l2trans.ps1,
tests/unit_void_value.lm2, tests/unit_bad_sizeof.lm2

SPEC 11.2.1 / 11.3.1  body `@: Foreign result` address slot
    DECIDED for this port. `@: LmP0Text result` in a method body
    is an explicit address-depth-1 machine slot, emitted as C
    `LmP0Text *` / L1 `@: LmP0Text l2_s{i}_{j}`, not OwnUsed, not
    an Lmx Structure/Array child, and not the lmx_own cache.
    `return: result` is the pointer VALUE. `return: @result` would
    be the address of the slot (dangling) and is not used.
    `result\data` / `result\length` are raw foreign follow of the
    frozen p0.h ABI (SPEC 7529–7532). ASK only if a later fixture
    needs a storage rule 11.2.1 / 11.3.1 do not settle.

SPEC 21.8  pointer and void results; sub:
    DECIDED as backend for this slice: result types
    `@: char` (3), `@: LmP0Text` (5), void (8, `sub:`).
    node==0 on a sub is `return`, not `return: 0`.
    Void used as a value is `incompatible entry signature`.

c.sizeof / lm_own_*
    DECIDED as known C-surface with exact signatures.
    `c.sizeof` admits `LmP0Text` / `c.LmP0Text` only; any other
    atom is `unknown foreign type`. lm_own_new_zero,
    lm_own_copy_bytes, lm_own_delete come from frozen
    `l1src/own.lm1` (new_zero / copy_bytes / delete / alloc_fails),
    imported as-is. That is heap allocation, not the lmx_own
    field-cache API. No implicit boxing or own promotion of the
    slot.

SPEC 21.5–21.6  checkpoint with empty OwnUsed
    DECIDED: actuals still checkpoint immediately before the
    executed call and before exit even when OwnUsed is empty.

Same-name formal + own bind (ticket 183000 / 183740)
    DECIDED in SPEC 11.3.1 / 21.5 / 21.6. Implemented for explicit
    char/size_t formals: from the top-level own-decl line the
    parameter is the cache (l2_p, not a second l2_q). No graph
    load over the incoming value. Dirty-only publish is to this
    method's node. Writes before the decl do not dirty. A formal
    without that bind is never published. Own-decl inside if is
    `unsupported own declaration`. Hidden / lexical-fallback
    parameters: next bounded patch, Unit 12. Explicit-only
    tests are not the completed general rule.

    @ after bind is settled: ordinary address-taking, the same C variable and
    lifetime, with the binding adding only checkpoint association (SPEC 11.3.1).
    L3 forbids @. This is not a pending question.

Unit 12 - hidden char/size_t through-args, closed direct calls
--------------------------------------------------------------
Files: l2trans.lm1, run_l2trans.ps1, tests/unit_own_only.lm2,
tests/unit_dyn_*.lm2

SPEC 21.3 / 21.5  membership — 192800 always-DynRequired WITHDRAWN
    Not a user question: hidden intern order is deterministic
    sorted source-name bytes; rec.sig includes the hidden
    contract. 21.3 already says so.

    21.3 excludes names satisfied by M's own binding. Own that
    supplies a callee enters OwnUsed; otherwise the name enters
    DynRequired. 21.5: own occurrence is materialized before
    entry and must not be silently reclassified as dynamic.
    `own-without-formal always DynRequired` is withdrawn. It
    would make every ordinary own field a caller-overridable
    input. The 11.3.1 bind is only when this method actually
    has that argument.

    Declaration-point analysis (this slice):
      - uses at/after `char: x` / `size_t: x`, no pre-decl
        free use, no explicit formal: own-only. Entry loads
        the materialized field. Not DynRequired.
      - bare use or assignment of x BEFORE that declaration,
        no explicit formal: those uses are not yet satisfied
        by own binding. x is DynRequired. From the decl line
        the same C variable is the own cache (11.3.1). The
        field is still materialized before entry; entry does
        not load it over the incoming hidden value.
      - bare x, no own, no formal: DynRequired only; never
        auto-published (21.3), unless a later bind exists.
      - explicit formal x: not DynRequired (183000 alias if
        own-decl follows).
      - callee needs x: if this caller already has own, formal,
        or hidden x, supply that; else x enters this caller's
        DynRequired.

    Types: not by matching the name across the unit. char vs
    size_t from this method's own decl, its explicit formal,
    a forwarded callee hidden type, or an existing typed
    context at that call-site. Literal 65 does not prove char
    and 3U does not prove size_t as a language rule. Mixed
    in one method is incompatible. Counterexample against
    unit-wide name guess: `fn: leaf () int / return: quote`
    beside `fn: holder () int / size_t: quote` must not make
    leaf's hidden quote size_t. Insufficient type info is
    `unresolved name`.

    Call site, known callee, closed unit:
      1. caller C variable for that name if present
      2. else load node\[0]x of the selected callee
         invoking_structure (historical immediate-only implementation; current
         lexical resolution may follow node parents)
      3. else reject that call
    `fn: main` stays the §1.7 adapter and does not grow
    DynRequired. Root value for a spliced first 21.8 call
    comes from that callee-node field if materialized, or
    from the test harness actuals. Do not push the need to
    main and reject the unit.

    Fallback in this slice: the unit Structure is
    `invoking_structure`. A quote child exists iff some method's
    OwnUsed materializes it. The leaf under test must
    free-use quote, not declare it. A sibling `holder` with
    `char: quote` is container data, labelled as such, not
    leaf's free use. `fn: leaf () int / char: quote / return: 0`
    does not consume quote as a free name and is not a
    fallback test.

    Physical ABI as 192800: node, declared formals, hidden
    in intern order. Empty DynRequired keeps today's intern.
    Caps at the time of this slice were 4 declared + 4 hidden.
    Unit 17 removes those as language/parameter limits; OwnUsed 4,
    method-count 8, intern-contract 16 remain implementation debt.
    Recursion remains an implementation limit. `@` after bind
    out of scope.

    Fixtures:
      unit_own_only.lm2     ordinary `char: quote` then assign;
                            no hidden; callers cannot override
      unit_dyn_leaf.lm2     leaf free-uses quote; outer own
                            quote supplies; leaf does not write
                            caller
      unit_dyn_mid.lm2      outer -> mid -> leaf; mid does not
                            mention quote; forwards hidden
      unit_dyn_predecl.lm2  leaf uses quote before `char: quote`;
                            hidden then bind; leaf field
                            published; caller source unchanged
      unit_dyn_fallback.lm2 leaf free-uses quote; sibling holder
                            materializes unit child; `typed`
                            (char formal) is the type supplier
                            at a real call-site; caller has no
                            source quote; adapter loads the
                            callee-node child then passes the
                            native C actual, not silent zero
      unit_dyn_sz.lm2       size_t free use typed by outer own
                            `size_t: value_length`, not by 3U
      unit_dyn_miss.lm2     negative: DynRequired, no caller
                            value, no callee-node child
      unit_dyn_type.lm2     negative: size_t own quote must not
                            type a sibling's char free use
      unit_dyn_cap.lm2      positive: five hidden names (4 own +
                            1 forwarded char formal); not a cap
      unit_dyn_early.lm2    return before own-decl does not publish

    Implemented in this slice (194400/203100). Closed-unit
    direct calls; char/size_t; intern
    includes hidden names sorted by source-name bytes then type;
    declared arity unchanged. `&&`/`||` with calls use the same
    l2_prep lowering as a statement call (hidden actuals,
    checkpoint after prep). Skipped RHS is a guarded if, not a
    C &&/|| with an inlined call. The old l2_inline_call path
    (node+declared only) is removed; emit_fields refuses to
    splice a method call into a C &&/|| expression.

    L2-to-L2 calls still pass the shared containing unit as
    `node`. That is allowed. Distinct-instance publish is a
    21.8 harness second Structure, not an ordinary source
    call and not a per-method graph layout. 21.3 fallback is
    the immediate callee-node child in this historical implementation.
    Current SPEC 21.3 permits lexical traversal through parents. `fn: main` stays
    §1.7. Fallback type for a free name is a real call-site
    formal/own supplier, not holder as a type oracle and not
    literal 65.

    Not in this patch: runtime-selected callable, int hidden,
    `@` of the bound name, arbitrary recursion, self-build.

Unit 13 - physical line scanners
--------------------------------
Files: l2trans.lm1 (bounded `while`), l2src/parser_physical_line.lm2,
run_l2trans.ps1, tests/unit_while.lm2, tests/unit_for.lm2,
tests/unit_continue.lm2, tests/unit_break.lm2

Implemented (205600 / 212200 / 213701). While is a keep-flag loop: each
iteration starts with full eval_cond (first, backedge, continue,
last false). Source atom `continue` is L1 continue of that while, so
it re-enters eval_cond; it is not a C continue of a cond-temp
header and not a checkpoint. Nested continue is innermost (two
whiles in one body, not a callee while). Frame `continue:` with
or without payload is `unsupported loop`, not a silent skip.
Continue outside while / `break` / non-cstyle `for` stay `unsupported loop`.
Established `for: int(i, 0) (i < 10) i++` is Unit 13b.
Keep/cond temps are backend locals, not LMX fields.

`count_line_breaks` plus a closed-unit copy of
`line_break_width_at` live in parser_physical_line.lm2 (7
methods, 4 OwnUsed: i, ch, count, width). unit_continue.lm2 is
a positive.

Frozen originals: stg/l1_baseline/l1src/parser.lm1
    lm_p0_index_is_line_start
    lm_p0_find_physical_line_end
    lm_p0_line_rest_is_horizontal_space
plus the two already-ported predicates they call
    (lm_p0_is_line_break, lm_p0_is_horizontal_space)
copied into the same closed unit (cap 8).

Not this unit: scan_indent, Document, indent stack, dash-fence enum,
Array, `break`. find_python_string_end moved to Unit 14.
visual_column moved to Unit 15.

Oracle: frozen parser.lm1 vs L2 drive.

Unit 13b - ordinary for Structure and int fields
------------------------------------------------
Files: l2trans.lm1, lmx_int.lm1, run_l2trans.ps1,
tests/unit_forj_path.lm2, tests/unit_forj_bare.lm2,
tests/unit_forj_parent.lm2, tests/unit_forj_nest.lm2,
tests/unit_forj_again.lm2, tests/unit_forj_graph.lm2,
tests/unit_forj_sib.lm2, tests/unit_printf_char.lm2,
tests/unit_printf_sz.lm2

Implemented (060900 lowering, 064800 graph publication).

SPEC  ordinary nested Structure fields / explicit path
    DECIDED by 044514/045200/060900/064800. The P0 tree of
    `for: int(i, 0) (i < 10) i++` is a 5-field frame; `int: j i`
    is `frame int(j, i)` inside the anonymous body; `for\j` is
    atoms for, \, j. Receiver: the for occurrence is a same-method
    Structure materialized as a unit-child BRANCH; header/body
    `int:` decls are typed children of that occurrence. Parent
    reads child j only as path `for\j` (graph load after dirty
    checkpoint). Bare parent `j` is `unresolved name`. Body may
    update a parent `int` field (inward). No new method activation,
    no per-iteration history, no C `for->j`. Sibling fors with the
    same field spelling have distinct Structures. Translator
    scope/for tables grow like existing own vectors; eight nested
    structures is not a language limit.

L1  compact `for(...)` header
    AVOIDED. Compact-header grammar is a separate unproved
    spelling (unit_forj_compact.lmx). This unit only lowers the
    established `for: int(i, 0)...` form. `for: a < b` stays
    `unsupported loop`.

ABI  LMX_TYPE_INT primitive pool
    DECIDED as the size_t pool discipline for this already-authorized
    own-int publication: unique mutable cells, stable addresses,
    no relocation. lmx_int_init/take/value/store. No new user
    semantic choice. Dirty-only store on checkpoint; prologue
    loads the graph payload. Foreign c.printf follows 21.7:
    declared actuals into typed temps, then dirty-only checkpoint,
    then the call. Explicit path reads observe previously published
    graph state; they are not a publish boundary. `end: for` is not
    a checkpoint.

Unit 14 - size_t* out-param and python string end
-------------------------------------------------
Files: l2trans.lm1, run_l2trans.ps1, parser_python_string.lm2,
tests/unit_sz_idx.lm2, tests/unit_sz_intp.lm2, tests/unit_sz_np.lm2

Implemented (222830). Formal type 9 is `@: size_t`. Store is
`name[0]:` through that formal only (P0 head `name[0]`). Other
indices and non-pointer `[0]` are `unsupported index`. `@: int`
/ `@: char` pointers are `incompatible entry signature`. A
type-9 actual is the pointer value forwarded; L2 `@ ident` is
not emitted. Test cells are L1/C harness `size_t` plus `@ cell`,
not L2 OwnUsed.

Closed unit parser_python_string.lm2 (cap 8):
    lm_p0_starts_python_string     copy of parser_text_starts_python
    lm_p0_find_python_string_end   frozen parser.lm1:713
    lm_p0_find_python_string_end_fwd  forwards out_end by name
    main

find OwnUsed: quote, i, run_length (3). Inner
`i + run_length < length && text[i + run_length] = quote` stays
call-free `&&` (C short-circuit, one index eval, no OOB).
skip_python_string_unchecked and L2 `@` of own are deferred
(graph-address / cache reload).

Frozen original: stg/l1_baseline/l1src/parser.lm1
    lm_p0_starts_python_string
    lm_p0_find_python_string_end

Next parser leftover after Unit 14: visual_column (Unit 15).

Unit 15 - visual column and indent tab
--------------------------------------
Files: parser_visual_column.lm2, run_l2trans.ps1, OPEN_POINTS.txt
No l2trans.lm1 change.

Implemented (000211 follow-on). Closed unit:
    lm_p0_indent_tab_column
    lm_p0_visual_column_between
    main

Numeric 9/13/10 for tab/CR/LF. Sequential CRLF: bound-guarded
`p+1` then the CR/LF ifs. continue already supported.
OwnUsed: p, column (2). Tab helper call checkpoints dirty
column then assigns and dirties again.

Frozen `((column / 8U) + 1U) * 8U` is restored. Nested groups used
to clobber global l2_lhs/l2_rhs; emit now uses per-call inner/piece
buffers. Overflow is `expression too long`, dest unchanged.

scan_indent is Unit 18. break not required. skip / L2 `@` of own
still deferred.

Frozen original: stg/l1_baseline/l1src/parser.lm1
    lm_p0_indent_tab_column
    lm_p0_visual_column_between

Unit 17 - source-sized declared and hidden arity
-----------------------------------------------
Files: l2trans.lm1, run_l2trans.ps1, tests/unit_arity*.lm2,
tests/unit_dyn_cap.lm2

Declared and hidden metadata are per-method malloc vectors, grown
to the source count (hidden grows during transitive analysis, then
canonical intern order). intern_store_mi copies name/length/type
rows without a fixed stride. Call actuals are malloc nact*256 and
nhid*256, streamed; no aggregate 256-byte call string. intern_pair
remains a 2-name probe. L2_FAIL_MALLOC injects the Nth malloc
failure; dest is unpublished. size overflow vs out of memory are
distinct diagnostics.

Not a language parameter cap: 9 and 127 are tests, not limits.
L2_FAIL_MALLOC injects the Nth l2_xmalloc; L2_ALLOC_LOG records
n/free/live/fail_at/fail_kind (1 formals, 2 hidden, 3 intern,
4 call-actuals). l2_release frees method and intern rows so live=0
after each process. Call-actual offsets use size_t. Hidden growth doubling is
checked against size_t overflow; cap that overshoots c.INT_MAX
while need still fits is clamped to need. int narrowing uses
c.INT_MAX, not a hardcoded 32-bit literal.

Remaining implementation debt, not this slice: 4 address slots,
16 intern contracts, 256-byte per-actual spelling, unsupported
recursion. OwnUsed and method tables are source-sized (Units 22–23).
scan_indent is Unit 18, not this unit.

Unit 18 - scan_indent_column parser port
----------------------------------------
Files: parser_scan_indent.lm2, run_l2trans.ps1, OPEN_POINTS.txt
Frozen ref: l1src/parser.lm1:442-457 (read-only)

Closed unit:
    lm_p0_is_horizontal_space
    lm_p0_indent_tab_column
    lm_p0_scan_indent_column   sub; two @: size_t outs
    main

Load ch after p<end. Both outs stored on the non-space stop and on
run-to-end. Early stop is a void `return` after the two [0]: stores
(Unit 19). Tab helper is the frozen parenthesized form. Numeric 9
for tab.

OwnUsed: p, column, ch (3). size_t* formals are not OwnUsed.
Test cells are harness-owned C addresses.

Not this unit: indent stack, dash-fence, Document, @own, break.

Unit 19 - void return in sub
----------------------------
Files: l2trans.lm1, run_l2trans.ps1, tests/unit_sub_ret.lm2,
tests/unit_sub_retval.lm2, tests/unit_fn_noret.lm2,
parser_scan_indent.lm2

Bare `return` (atom) is admitted only when the current method is
sub (l2_m_ret=8). Emit dirty-only checkpoint then `return`, so
own fields publish before exit; nested if/while use the same
emit_body path. Fallthrough off the end of a sub also checkpoints.
`return: value` in sub and empty `return` in a value-returning fn
stay unsupported body.

Unit 20 - line_start / line_rest
--------------------------------
Files: parser_line_start.lm2, run_l2trans.ps1, OPEN_POINTS.txt
Frozen ref: l1src/parser.lm1:545-560 (read-only)

Closed unit:
    lm_p0_is_horizontal_space
    lm_p0_index_is_line_start
    lm_p0_line_rest_is_horizontal_space
    main

index=0 returns 1 before any index-1 read. Out-of-range positive
indexes are not given invented behavior. line_rest loads `ch:
source[i]` then calls the helper (L2 rejects indexed actuals).
Numeric 32/9/10/13. OwnUsed: i, ch (2).

24-case differential vs frozen parser.lm1: empty/null zero index,
first byte, LF/CR/CRLF positions, ordinary chars/NUL, empty or
reversed start/end, spaces/tabs/all-whitespace/nonspace boundary
and nonzero start. Suite: l2trans gen2 ok.

Not this unit: position_in_slice (Unit 21),
find_physical_line_end (parser_physical_line.lm2), Document.

Unit 21 - position_in_slice / line_break_width_at
------------------------------------------------
Files: parser_position.lm2, run_l2trans.ps1, OPEN_POINTS.txt
Frozen ref: l1src/parser.lm1:503-527 and parser_text.lm1:27-36
(read-only)

Closed unit:
    lm_p0_line_break_width_at
    lm_p0_position_in_slice   sub; two @: size_t outs
    main

CRLF width is 2; a slice that ends on CR is width 1 (helper length
is the clamped index, not the full text length). Position assigns
the size_t formal `index: length` then walks i < index, passing
that clamped index as helper length. continue on a break. Tab/NUL
increment the byte column, not visual_column. OwnUsed: i, line,
column, width (4). size_t* formals are not OwnUsed.

Differential vs frozen parser.lm1 includes empty/null-zero, clamp
above length, before/inside/after CRLF, standalone CR/LF, multiple
lines, non-default base, ordinary/NUL/tab, trailing lone CR, and
a CRLF cut between CR and LF. Suite: l2trans gen2 ok.

Not this unit: Document, indent stack, L2 module import, method-cap lift.
position_in_slice is also in parser_physical_line.lm2 (Unit 22 merge).

Unit 22 - source-sized OwnUsed and layout merge
----------------------------------------------
Files: l2trans.lm1, run_l2trans.ps1, parser_physical_line.lm2,
tests/unit_own5.lm2, tests/unit_own6.lm2, tests/unit_own_clash.lm2,
OPEN_POINTS.txt, README.txt

OwnUsed name/type/live and per-method uses/alias/pre grow to the
source count (malloc, kind 5). seen membership is a source-sized
int vector, not `int` bits (`1 << oi`). Address-slot stride stays 4.
Zero OwnUsed is valid. Aliases init -1; uses/pre/live init 0.
Overflow vs OOM distinct. l2_release frees the tables.

parser_physical_line.lm2 now holds position_in_slice as the 8th
non-main helper (OwnUsed union i,ch,count,width,line,column).
Isolated parser_position.lm2 kept.

Not this unit: intern 16, address 4, recursion, predef.
Method tables are source-sized (Unit 23).

Unit 23 - source-sized methods and layout helper merge
------------------------------------------------------
Files: l2trans.lm1, run_l2trans.ps1, parser_physical_line.lm2,
tests/unit_own32_pub.lm2, OPEN_POINTS.txt, README.txt

Method metadata, call-graph, and address-slot rows (stride 4)
grow to the source method count (alloc kind 6). Main is not
counted. unit_nine is a passing 9-method test. 17 and 33
methods share one intern contract so intern16 cannot mask the
method test. Recursion/cycle diagnostics unchanged.

parser_physical_line.lm2 also holds indent_tab_column,
visual_column_between, and scan_indent_column. Isolated
parser_visual_column.lm2 / parser_scan_indent.lm2 kept; their
differentials also run against the merged unit.

Remaining blockers for full parser migration: intern contracts
16, address slots 4, 256-byte actual spelling, unsupported
recursion, unimplemented L2 predef, size_t cell pool 16
(lmx_size_init).

Unit 24 - OwnUsed/method growth corrections
------------------------------------------
Files: l2trans.lm1, run_l2trans.ps1, tests/unit_own32_pub.lm2,
tests/unit_own_grow_alias.lm2, tests/unit_own_meth.lm2,
OPEN_POINTS.txt, README.txt

OwnUsed copy on grow walks allocated method rows (mi < l2_m_cap),
including the under-construction collect_decls row (l2_m_n not
yet incremented). Method reserve restripe of OwnUsed is
fail-atomic: cap*own_cap*sizeof(int) via l2_mul3_ok, else overflow
and the old tables stay; l2_m_cap is not bumped. l2_m_n is checked
against INT_MAX before l2_m_n+1. OwnUsed seed is 4.

Executed: m16/m32 via splice-drive of generated prototypes
(Get-L2Call); m16↔m32 cycle rejected; bind32 writes n32=3 before
own-decl then n32:n32 after so the graph checkpoint is 3, then
set32 publishes 65; n00 stays 1. Formal alias on the first method
survives OwnUsed 4→8 (n00..n08). Hidden quote through m16/m32
(shared intern on m0 = int a,b; intern_prove is coupled to that
shape). fail-malloc kind 6 while own rows exist (unit_own_meth).
README arity 1–4 claim removed (Unit 17 already source-sized
params). scan_ident does not re-mark uses.

lm_p0_advance_layout_line is in parser_physical_line.lm2 (181710).
It reuses count_line_breaks + line_break_width_at; @: size_t
offset/line are the two explicit pointer outputs, including
line[0]: line[0] + n. Nested helper calls stay in-unit.
Differential vs parser.lm1: no final newline, LF, CRLF, lone CR,
embedded breaks, zero-width, empty, NUL, sequential advances.
physical_line intern contracts now include this sub (l2_m11).

Unit 27 - trailer_role classification (LmP0TrailerRole)
------------------------------------------------------
Files: l2trans.lm1, parser_trailer_role.lm2, run_l2trans.ps1,
OPEN_POINTS.txt, README.txt

LmP0TrailerRole is admitted as L2 formal/result type 10 from
l1src/p0.lm1.h (typedef int; NONE=0 DASH_CUTTER=1 END=2 RETURN=3
UNTIL=4). Not intern16. l2_need_p0 includes the generated header.

Leaf ports: text_has_prefix_name, legacy_trailer_role,
from_payload, payload, is_tail_cutter, trailer_role.
Dash cutter is the leading-dashes + space/# path only.
AVOIDED: brace-mark dash lines (`--- { ... }`) need
lm_p0_scan_brace_mark_unchecked and are not this unit.
AVOIDED: Document / indent stack; trailer_role_accepts_target
(registry + LmP0Node). Arises when those scanners are ported.

Differential vs parser.lm1 on prefix/legacy/role/from_payload/
payload/is_tail_cutter plus `---` / `--- #` / 81-dash too-long.
Next: Document / indent stack, then remaining parser.lm1 scanners.

Unit 28 - copy-only Message mailbox (19.29.7.1)
----------------------------------------------
Files: l2src/lmx_message.h, lmx_message.lm1, lmx_message_selftest.lm1,
run_lmx.ps1, Lingvamyxa_spec.txt 19.29.7.1

Create/send/stop/end_turn/pump/recv/fail/poll/set_now/drive.
Transport-owned copy; admission enqueues inbox; recv materializes
at turn entry. Dedup (recipient, sender, id). Create key
(parent, create_id). KIND_STOP is internal close control (not
enqueued). KIND_CANCELLED is ordinary result data and does not
close the recipient. Normal closing end_turn requests children
close (flag only, not STOPPED, child outbox kept). Poll is the
abrupt-parent fallback (`who` is the child; 0 walks non-roots).
Drive is the host loop: set_now + poll(0) + idle closing
end_turn + pump. Instant stop_descendants is gone. KIND_DEAD is
runtime-posted, not dying outbox. AVOIDED: graph-copy, OS thread
per Message, durable dedup, remote silence as death,
KIND_REJECTED emission, done[32] silent forget. SPEC 21.13 throw
payload not this unit. Mix placement path is parent
counter + append (not LmxMsgAddr, not packed Java int).
Prototype PATH_CAP 16 refuses rather than wrap. Not
routing/rights. Not every Mix cell is a running Message.

Unit 28b - host ingress V0 (092900)
-----------------------------------
Files: lmx_message.h, lmx_message.lm1, lmx_message_host.h,
lmx_message_host.c, lmx_message_selftest.lm1,
lmx_message_host_selftest.c, run_lmx.ps1, LMX_MSG_HOST_INGRESS_V0.txt

DECIDED: owner thread is the caller of runtime_new. Foreign threads
may only host_post. Mutex+wake protect the host queue only
(Win32 CRITICAL_SECTION+auto-reset Event; POSIX mutex+cond).
Copy/envelope/queue policy is L1 (lmx_msg_host_post). Platform C
is lock/wake/owner/shutdown flag. host_post copies once under the
mutex and returns STAGED. dest lookup is owner drain/admit.
nonzero n with null bytes is INVALID. Wait timeout_ms=0 polls;
0xFFFFFFFF is refused (no infinite wait). Wake fail after enqueue
is still STAGED; owner drains on a bounded tick.
Lifetime: stop admitting new callbacks, shutdown, join every
in-flight producer/callback, then delete; rt stays valid until
that join barrier. path_n returns -1 off-owner; path_seg/init_copy
return INVALID; delete off-owner is a void no-op.
Win32 concurrent test run here. POSIX primitives written, not
executed on this machine. Test injectors are -DLMX_MSG_HOST_TEST
only; production host.o has get_nomem==0 and no setters.
Remaining limitation of ingress V0: not concurrent Message
execution. done[32] still exists; host events use id=0.
AVOIDED: app C locks; new KIND enums. ASK none.

Unit 28c - overlapping Message turns (100400)
---------------------------------------------
Files: lmx_message_exec.c/.h, lmx_message.h/.lm1,
lmx_message_exec_selftest.c, LMX_MSG_EXEC_HOST_V0.txt, run_lmx.ps1
DECIDED: per-Message turn latch; workers for ANY affinity; UI
affinity on owner thread; runtime mutex not held during turn_fn.
Table is LmxMsg** (stable Message addresses). recv/send/end_turn
require the latch once workers are started; serial owner fallback
when nworkers==0. Win32 overlap: slow Sleep(80) and fast recv in
the same window; 20 creates during the slow turn. POSIX workers
written, not run here. V0 ingress unchanged. AVOIDED: global
handler mutex; creator bypass of another Message's latch.

Unit 29 - fence line matchers (block-string = / raw-comment *)
-------------------------------------------------------------
Files: parser_fence_line.lm2, run_l2trans.ps1, OPEN_POINTS.txt,
README.txt
Frozen ref: l1src/parser.lm1:820-858 (read-only)

Closed unit:
    lm_p0_is_horizontal_space
    lm_p0_line_rest_is_horizontal_space
    lm_p0_match_block_string_fence_line
    lm_p0_match_raw_comment_fence_line
    main

80U is LM_P0_MAX_FENCE_LENGTH. Indexed source bytes are loaded
into `ch` (L2 rejects indexed actuals). Extra fence char after
the counted run rejects. Trailing horizontal space is admitted;
any other trailer rejects. AVOIDED: Document, indent stack,
dash-fence status enum, brace-mark (`--- { }`), skip_fence_block.
Arises when those scanners are ported.

Differential vs parser.lm1 on empty, short, exact, extra-char,
wrong rune, indented start, tab/space trailer, 80 vs 81.

Unit 30 - layout_prefix_is_deeper
---------------------------------
Files: parser_layout_deeper.lm2, run_l2trans.ps1, OPEN_POINTS.txt
Frozen ref: l1src/parser.lm1:768-771 (read-only)

If either dot_level is nonzero, compare only dots; else compare
indent columns. AVOIDED: scan_layout_prefix, Document, indent stack.
Next: dash-fence status / brace-mark unchecked, then Document.

Unit 25 - P0 close consumption (return trailer, end:name/fn, ---)
----------------------------------------------------------------
Files: l2trans.lm1, run_l2trans.ps1, tests/unit_ret_tr*.lm2,
tests/unit_end_*.lm2, tests/unit_main_ret_tr.lm2,
tests/entry_ret_tr.lm2, tests/l1/fn_close_*.lm1,
tests/l1/run_legacy_p0.ps1, tests/l1/legacy_p0_manifest.txt,
tests/l1/run_smoke.ps1, OPEN_POINTS.txt, README.txt

L2 now consumes fn/sub/main closers as P0 trailers, not as a
hard `end:<function-name>` requirement:
  - end:<canonical name> (priority)
  - end:fn / end:sub (receiver/head)
  - return:<expr> as the function trailer (empty or nonempty body)
  - missing trailer / --- source-level cut (P0 already closed the frame)
Wrong end target: "end target does not match close target".
Nested `end: return` on a return statement stays unsupported trailer
(entry_trailer). Trailer expression is scanned, checked, evaluated
and checkpointed like a body return (own publish, hidden discovery).
Bare `end` stays banned (v15) as a separate rule; it is not a
nullary operation. Empty `end:` is one instance of the general
P0 empty-colon syntax rule (112600, superseding 112500's
translator-only placement): a colon Frame with no argument
fields after body assembly is a current-parser P0 error;
valid nullary is written without ':'; compact `f()` and `()`
stay allowed. Historical 620db86 P0 still accepts
tests/empty_colon_frames.lmx (versioned old acceptance, not a
port regression). Root L1 l1_emit_fn already rejects empty end:
(`end trailer expects exactly one target name`) and wrong
targets (`end target does not match close target`). Making
lm_p0_validate_nonempty_colon_frames actually reject in the
new parser is the remaining implementation. Frozen STG P0 is
not silently edited. L2 may keep a marked temporary defensive
check until coordinated STG promotion. Full
self-build/next/check was not rerun in the Unit 25 window.
Main-only nonempty body + return trailer dropped the last body
puts (110800); that is a remaining L2 leaf-path bug, not a
completed gate. --- is a multi-level tail cutter in P0; parser
source is unchanged.

Paired positives: tests/l1/fn_close_name/fn/ret/dash and L2
unit_ret_tr / unit_end_*. invalid_fn_end_wrong and
invalid_fn_end_empty are root negatives.

Legacy parse gate: tests/l1/run_legacy_p0.ps1 vs printTree.lm0
oracle, root gen2 and STG gen2. Parse only; consumer omissions
are the L1/L2 tests above. Inline `prototype:` drop and const:
qualifier-distribution remain pending (not this unit).
advance_layout_line landed in parser_physical_line.lm2 (181710).

Unit 26 - triple-quote A+1 value decode
--------------------------------------
Files: l1src/l1trans.lm1, lm2/l1trans.lm2, lm1/build/l1trans.lm1.c,
l2trans.lm1, run_l2trans.ps1, tests/l1/quote_run.lm1,
tests/entry_puts_triple.lm2, tests/entry_ret_tr_puts.lm2,
tests/entry_ret_tr_two.lm2, tests/entry_ret_tr_bad.lm2,
OPEN_POINTS.txt

Triple-quoted literals (N=3, both " and ') decode body runs of the
opening quote: source run R>=4 means value R-1 (A>=3 encoded as
A+1). R=1,2,3 stay as written. Opposite quote, backslash, and
equals/star fences are not shortened. P0 spelling is unchanged.
Root L1 l1_emit_atom_node uses l1_emit_triple_body then
l1_emit_c99_string. L2 admits """...""" where ordinary "..." was
already allowed and lowers by decoding once to an ordinary L1
string (no double decode; frozen STG L1 still works).

Main-only return-trailer with nonempty puts body now validates all
body puts and emits them; last-field-is-return only when the
return lives in the body. Invalid sole body + trailer still
rejected.

quote_run.lm1 runtime: a"""b / a'''b / x"y / x""y / x""""y /
x"""""y / a'b / a\b. entry_puts_triple: a"""b. entry_ret_tr_puts:
MUST PRINT then return 0.

STG L1 source still frozen. Full run_seed/next/check not the
whole-project gate this window. 111200 common P0 tree-contract initial batch is in
tests/p0_tree_contract (620db86 goldens; root printTree match 19/19
plus C_nested_short reject). C_nested_short is a resolved historical
prose error: the two-dot line after inline ShortForm1/ShortForm2 is
an expected P0 error 13 at 2:5; keep the negative fixture. The
corrected vertical spelling is a pending positive counterpart
(reference-first in old tests/p0_tree_contract). Empty-colon is a
current-parser P0 syntax error; 620db86 still accepts
tests/empty_colon_frames.lmx. Ticket 112500 translator-only
placement is superseded. Then advance_layout_line.

Unit 16 - nested expression emit scratch
----------------------------------------
Files: l2trans.lm1, run_l2trans.ps1, parser_visual_column.lm2,
tests/unit_paren_*.lm2

l2_prep/l2_emit_fields no longer write l2_lhs/l2_rhs. Each
recursive emit uses a local 256-byte piece/inner. l2_tok remains
emit_body's statement dest. l2_cat overflow → expression too long
at the source node; dest not published. Tab helper spelling is
the frozen parenthesized form again.

`return: 0` on a malformed native node stays the runtime
adapter, not Type/throw admission. Not expanded here.

P0-as-Lmx, remaining parser.lm1 scanners, and L2 self-build
    Not started. Frozen p0.h / l1src/own.lm1 / bootstrap / root L1
    unchanged. parser_text_heap.lm2 does not replace l1src.

Decisions carried forward (resolved 2026-09-12)
----------------------------------------------
ABI 14.1/14.2  Children are void * pointers into typed arrays/ranges;
               Structure.len is only child count. Array length is separate.
ABI 14.4/14.5  Auxiliary address -> short source name; no execution identity,
               collision policy or mandatory occurrence registration.
ABI 14.6       Merge and Message creation share the complete-used-graph traversal
               to node = 0. Explicit destination node/reference fixups preserve
               aliases/cycles. independent supplies zero. Result node identifies
               the Structure containing merge. No pointer-only merge exception.
ABI 14.7       Copy all used language-owned payloads and references, preserving
               aliases/cycles. Methods and their immutable descriptors are shared;
               graph/node copying retains the same method-descriptor references.
ABI 14.10 /
SPEC 21.13.4   Status plus typed result/throw out-parameters, distinct declared
               throw and runtime failure, no ordinary-result store on throw,
               activation/Message-owned storage. longjmp only for assert.
SPEC 6.6.2     Native C arithmetic follows C, including unsigned u64 modulo;
               other VMs retain native behavior. Existing bounds/allocation
               checks remain. No new arithmetic mode or pending u64 decision.

Implementation of these contracts is work for the core owner, not an unanswered
language question. Consult the current source sections before opening any ASK.

Unit l2_tok_text triple lowering (164650)
----------------------------------------
SPEC 3.4.4.1 / old lm2 parser / current P0: inline Python-like
    strings use EXACTLY THREE opening and closing quotes. Variable
    exact-match fence length is for ===/*** blocks, not these
    literals. AVOIDED: L2 lowering uses fixed N=3; body runs >=4
    lose one quote. `""""hello""""` is specified-unterminated
    (opener takes three; a four-quote trailing run is body, not a
    closer) — tests/entry_puts_triple_fence4.lm2. Instruction
    error in 163500 called that a P0 gap; it is not.
    Body-leading quotes: `""""hello"""` → `"hello`; seven opening
    quotes plus closer encode three body quotes. Both quote kinds.
    l2_tok_text dest is 256 in every caller (l2_tok, piece, s0-s3,
    l2_act_at slots). ===/*** comment fences untouched.
    171100: producers include generated l1src/p0.lm1.h (l2_text_hash,
    l2trans emit). run_l2trans no longer predefines LM_H_… when a TU
    also named leftover l1src/p0.h. fmt_buf.c links l2trans.c with
    main renamed and calls l2_fmt_l1_string / l2_tok_text: empty,
    exact fit, one-byte overflow including escape expansion, NUL /
    control / high bytes, both quote kinds, decoded bytes checked,
    overflow rejected without writing past cap. Puts stream tests
    remain; they are not this buffer contract.

Candidate identity (180930, not default STG)
--------------------------------------------
    Named L1_GEN=rootC244 is
    stg/l1_baseline/build/l1trans/rootC244/l1trans.exe
    SHA256 85C0D959565C47B8801E9D8149DD13C397E35C7406758D69E8A10A2B0AD58E43
    (root tracked C 4626B57B… / source 6b58e51). Default STG gen2 remains
    8C69F4ED…. L2 suites on this exact exe: l2trans_suite.log /
    lmx_suite.log under build/l1trans/logs/rootC244/. control_goto is
    still P0 error 32; not skipped.

Promotion completion (181210)
-----------------------------
    After agreed verification AND actual replacement of default STG
    gen2, notify Claude in English inbox: exact stable exe path, source
    revision, SHA256, mixa runner command/env, which candidate became
    stable, whether a candidate override must be removed. Put that
    inbox id in the Grok completion reply. Do not call rootC244 the
    default before that replacement. Do not require Claude to rebuild
    or wait on his ACK.

Unit dash-fence status (011330, after da77016)
----------------------------------------------
Files: parser_dash_fence.lm2, run_l2trans.ps1

SPEC 3.4 dash delimiter line / P0 fence length 80.
    DECIDED for the classifier: NONE=0 VALID=1 TOO_LONG=2
    TRAILING_TEXT=3, max 80 dashes. Whitespace and `#` comment after
    the dashes are VALID. Copied is_horizontal_space / is_line_break
    must match parser_text_predicates.lm2.

ABI / remaining brace-mark scanner
    DONE (072100): helper is frozen-parity `lm_p0_scan_brace_mark_unchecked`
    with `@: int closed`. Unclosed returns i and closed=0; matching `}`
    returns i and closed=1. dash_fence_status observes the callee write
    via explicit `node\closed` (21.7: @: own passes the graph cell; the
    caller cache is not reloaded). Hang guards kept: if skip_python /
    scan_c_char does not advance, i += 1 (042800). Differential vs
    parser.lm1 is brace_l2_drive plus the existing dash_l2_drive.
    `node\field` expression path loads are admitted (unit_node_path).
    `@: int` formals already lower (unit_sz_intp). `@: local` actuals
    already lower (unit_addr_take / unit_addr_arg).
    Next: Document / indent stack. Live gen2 l1trans/printTree stay L1.

Unit indent stack / indent_level_from_column (074800)
-----------------------------------------------------
Files: parser_indent_stack.lm2, l2trans.lm1, run_l2trans.ps1, OPEN_POINTS.txt
Frozen REF: l1src/parser.lm1 indent stack family + indent_level_from_column
(read-only).

L2 now admits @: LmP0Document, @: LmP0IndentStack, const @(LmP0IndentStack)
as p0.lm1.h migration types (13/14/15), @: void (16), IndentStack result,
fields columns/count/capacity, lm_own_resize, c.memset/c.memcpy,
c.sizeof(c.LmP0IndentStack)/c.size_t, and c.lm_p0_set_diagnostic (leaf
definition emitted; candidate link must use parser's).

Parse entry that uses this stack: lm_p0_indent_level_from_column
(parser.lm1:1030), called from field/layout parse inside lm_p0_parse_bytes.
Differential indent_l2_drive vs parser.lm1: col 0/4/4/0/2 then a second
stack 0/4/2 unindent mismatch (diag 17). Live gen2 printTree/l1trans stay L1.

Remaining L1 on the candidate parse path: lm_p0_parse_bytes / parse_file,
document_init/destroy, scan_layout_prefix, field parse loop, empty-colon
error 32, dump. Frozen REF unmodified. Empty-colon vs 620db86 distinction
stays (error 32).

Unit candidate parse_bytes uses L2 indent (082500)
-------------------------------------------------
Files: run_candidate_indent.ps1, indent_stack_abi.c, indent_parse_probe.c,
run_l2trans.ps1, OPEN_POINTS.txt
Frozen REF: stg/l1_baseline/l1src/parser.lm1 unmodified.
Installed gen2 printTree / l1trans not rebuilt.

A candidate parser.lm1 copy has the ten indent-stack / indent_level
definitions stripped (fail if missing, duplicated, or a definition
survives; prototypes remain). Generated L2 exports are objcopy-kept
(l2_*, lmx_*, l2_indent_boot) and C ABI adapters in indent_stack_abi.c
provide the frozen symbol names so L1 parse_bytes calls l2_m10 for
indent_level_from_column. indent_parse_probe: parse=0 hits>=1.

Growth/clone/pop (indent_l2_drive vs parser.lm1): grow 41 64 40,
clone 41 64, mut 42 41, pop 1 0.

Pinned old parser: git 620db8612c32569c8dd507cca135d5d076144e9f
printTree_620db86.exe SHA-256
CB564AD6FF52F35E918FFBAE6A6E2E166147DADCAAE445F9D5A2526A77801EF3.
build/lm0/printTree.lm0.exe is a different hash and is not the oracle.
Trees: candidate == STG gen2 on the p0_tree_contract corpus plus
tests/arr.lmx and tests/tail_cutters.lmx. Accept fixtures still matching
620 goldens must keep matching (no new tree deltas absorbed). Tracked
rejects: C_nested_short (13), empty-colon 32
(return_colon_empty_trailer, return_colon_comment_trailer, F_star_fence),
invalid_eq_unclosed (20), invalid_triple_unclosed (4).

DECIDED: empty-colon rejection is the agreed current-vs-620 delta.
AVOIDED: live gen2 / frozen parser.lm1 rewrite.

Next bounded removal after 082500 was scan_layout_prefix (090841).

Unit candidate parse_bytes uses L2 scan_layout_prefix (090841)
--------------------------------------------------------------
Files: parser_scan_layout_prefix.lm2, layout_prefix_abi.c,
run_candidate_indent.ps1, run_l2trans.ps1, indent_parse_probe.c,
OPEN_POINTS.txt
Frozen REF and installed gen2 untouched.

L2 port of lm_p0_scan_layout_prefix on top of parser_scan_indent
helpers (byte-identical helper bodies). Differential slp_l2_drive
matches parser.lm1. Address actuals of own size_t caches are not
used: the callee writes through the existing @: size_t out formals,
then the caller reads out_offset[0] / out_indent_column[0] into
locals. Dot is numeric 46 (L2 has no '.' literal here). Inner space
skip uses a more-flag because L2 rejects break and a call inside &&.

Candidate strip adds lm_p0_scan_layout_prefix. Second L2 object
redefines l2_m3 -> l2_layout_m3 and keep-globals only
l2_layout_boot / l2_layout_unit / l2_layout_m3 so it does not collide
with indent_stack's l2_m*. Probe: parse=0 indent_hits>=1 layout_hits>=1.

Corpus (separated, not overlapping counts):
  total 38 = p0_tree_contract 36 + extra arr/tail_cutters 2
  accept 32 (match620 30 + extra_no_golden 2)
  expected_reject 6 = empty_colon_delta 3 + same_as_620_reject 3
  known_tree_delta_vs_620 0
Pinned 620 printTree hash unchanged. Live l1trans 65D5A5ED.

DECIDED: empty-colon 32 remains the only current-vs-620 parse delta.
AVOIDED: live gen2 / frozen parser.lm1 rewrite; two L2 units sharing
global l2_m*.

Next L1 on the candidate path: parse_bytes / parse_file,
document_init/destroy, field parse loop, empty-colon 32, dump.

Unit 28d - per-context Message ownership (032700 / 033000)
----------------------------------------------------------
Files: LMX_MSG_CONTEXT_V0.txt, lmx_message.h, lmx_message.lm1,
lmx_message_selftest.lm1, Lingvamyxa_spec.txt 19.28.R2.2 / 19.28.R2.6 /
19.29.6 / 19.29.7.1, OPEN_POINTS.txt

SPEC 19.29.6 worker-pool / migration
    DECIDED by user: each live Message owns mailbox, parent, and
    direct-child list. A language-level scheduler/router, if needed,
    is itself a Message. OS wait/wake/mutex and OS threads are
    substrate. A C worker pool plus process-wide Message table is
    not the architecture. EXEC_HOST_V0 is prototype evidence, not a
    pin. Erlang/OTP/Akka/Pony are orientation only (no BEAM lookup,
    no Akka dispatcher, no Pony scheduler as LMX rules).

Direct-child list
    DECIDED: first_child / next_sibling / parent_msg. create links;
    close/rollback walk that list. Independent parent families do
    not share lists. tab[] / integer find remain deviation D1 until
    send is capability-only.

ASK A1  DECIDED 034300 / 19.29.7.1: staged copies drop at admit if
    dest is gone; no reply_to bounce. Closed handle is the still-
    allocated DEAD/STOPPED/RELEASED object; addr is not reused.
ASK A2  DECIDED 035400: lmx_msg_exec_start_contexts is one OS
    wait-thread per ANY-affine bound context; UI stays on owner
    ui_step. Not a worker pool and not a Scheduler Message.

AVOIDED: mixa_manager/vendor overwrite; frozen l1trans rebuild;
app pin of EXEC. Ingress V0 remains the host_post pin.
