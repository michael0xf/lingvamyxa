Claude assignment -- effective 2026-09-11
========================================

The user transferred OpenCode's entire remaining mixa_manager workload to this
channel. Claude is active again; OpenCode monitoring remains disabled. The old
simple-tasks-only / refuse-large-tasks / quota-pause assignment is superseded.

Workspace: C:\Nyasha_Planet\lingvamyxa
Channel: work_chat\claude\inbox, outbox, seen
Read PROTOCOL.txt, INBOX_WATCHERS.md and OPENCODE_HANDOFF_20260911.md.

Own the app backlog in sequential, bounded, verified stages. Preserve existing
work and inspect exact paths before editing. Codex and Grok retain L1/L2 core,
Message runtime and stable promotion; do not edit their active files or rebuild
their compiler. Do not wait for Message to port independent app algorithms.
Ask Codex a concrete question for a genuine missing language/ABI decision or an
overlapping file, while continuing independent agreed work.

All new application logic and test logic is L1, headers name.h.lm1. Isolate OS
ABI glue; do not add application-private threads, locks, atomics or shared state.
The application will use Message for concurrency. Existing approved foreign
libraries are not a license to move app algorithms into handwritten C.

Use the pinned stable compiler read-only and unique build/mixa/claude outputs.
Run focused acceptance tests, not a compiler self-build for each app change.
Commit and push every verified stage using exact owned paths; preserve unrelated
dirty files and never force-push, reset or stage the whole shared tree.

English in colleague correspondence and progress notes. Give short substantive
progress while working. An acknowledgement or plan is WORKING, not completion.
A blocker names the exact failing step, attempted checks and necessary decision.
DONE reports changes, tests, commit and limitations. STAGE DONE means continue
to the next already agreed stage without waiting for a reminder.

Use inbox FSW plus a 30-minute fallback as described in INBOX_WATCHERS.md.
Do not declare it healthy until a real inbox publication wakes this session and
produces the matching outbox reply. Never reactivate OpenCode or Antigravity.
