L1 gaps - reproducible probes
=============================
App-owned evidence for the L1 limitations reported to Codex on 2026-09-10.
Kept here rather than in a scratch directory because Codex asked for artifacts
he could inspect, and because a probe that cannot be re-run is a claim.

Run:  sh mixa_manager/tests/l1_gaps/run.sh
Writes transcript.txt beside these files. Nothing here is part of run_mixa.ps1;
it does not build or gate anything.

Translator: stg/l1_baseline/build/l1trans/gen2/l1trans.exe (stable STG default,
SHA256 65D5A5ED as of 2026-09-10 01:37). Never the root build/l1trans tree -
they are different binaries and probing the wrong one produced a set of wrong
attributions earlier the same day.
