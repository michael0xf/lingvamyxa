Mixa Manager — current handoff
=============================
Updated 2026-09-10, morning, America/Sao_Paulo. This replaces the old Task25
in-flight handoff; earlier decisions and investigations remain in Git and the
subject documents. Recheck Git, latest inbox/outbox and saved logs before acting.

1. Goal and owners
------------------
Port ClearShell functionality/appearance with the owner's per-element deltas.
PORT_OF_CLEARSHELL.txt is the framing document; subject docs own the details.
Do not simplify the unusual navigation or FSync selection model by analogy.

Codex coordinates grok_bot on this app. Grok owns the independent core L2 lane.
Claude's reference investigation remains important evidence; no active app
ticket is assigned to him and app progress does not depend on his availability.
Colleague messages are English. The user's discussion is Russian.

Codex monitors grok/outbox and grok_bot/outbox with a FileSystemWatcher and a
30-minute backup poll. Current ticket state comes from matching inbox/seen/
outbox, not merely an old reply timestamp. Watcher state is outside the repo.

2. Verified implementation checkpoints
--------------------------------------
Task24/25 COMMITTED AND PUSHED: 75ca805.
  Tiles/buttons, per-handle backend dispatch, L1 constructor registries,
  concurrent backend handles, even physical cells and shared text/UI font.
  The handwritten C registry was removed; its typedef bridge remains in the
  permitted ABI header. 2a70b34 strengthens negative L1 diagnostic fixtures.

Task26 REVIEWED, NOT YET COMMITTED at this snapshot.
  One moving double-box frame, general MIXA_DRAW_KEEP_BG, snapshot restoration.
  Snapshot is bound to its target rectangle/storage/geometry; mismatched
  clear/move fails before writes. Clear before underlying redraw or resize.
  Same-target overlap and two-target rejection tests are saved in:
    build/mixa/logs/task26_bind_20260910.log
    build/mixa/logs/task26_bind_headless_only_20260910.log
  Both contain successful app/highlight test results. This is logic/testing
  acceptance, not proof of the complete visual application.

Task27 LOGIC REVIEWED; corrective checkpoint 080500 accepted, not committed.
  Eight last-displacement octants; no smoothing/latch; initial down-right.
  Mixed-axis failure now computes both coordinates before publishing either.
  Coordinate publication and mixed-axis failure tests were inspected together
  with both saved logs. This is not visual completion. Saved test logs:
    build/mixa/logs/task27_windows_full.log
    build/mixa/logs/task27_headless_only_full.log
  The current selected Windows pointer font was measured as MS Gothic,
  tmHeight18/ascent15. All eight arrow glyph indices were absent (0xFFFF),
  while ASCII A in the same font rasterized. This is a finding about that
  selected font, not Windows in general. No fallback has been chosen.
  A private glyph probe also exposed incorrect backend error-sentinel checks;
  these are not the cause of the measured absent arrow indices.

Shared unfinished app work belongs to grok_bot: highlight/draw, pointer and
their tests, plus run_mixa.ps1 integration. Preserve it. Core stg/l1_baseline/
l2src and root tests/return_*.lmx belong to other work. Never stage them with
an app checkpoint. A previous automation policy blocked grok_bot's Git push;
report any current block accurately and do not retry or route around it.

3. Next work
------------
Correct the bounded backend error checks and report glyph/fallback choices,
without inventing replacements. Then implement the PROCESS_SEAM for FIRST_VERSION
mode0.1.2: command execution with nonblocking output into a file-backed console,
cursor/character wrap/editing and one F1 Help button. No terminal emulation.
Independent process work need not wait for a pointer-font product decision.
Concurrency is not deferred: implement jobs and asynchronous system-event
handoffs through the Message boundary (PROCESS_SEAM6). Review the current
prototype's ingress/host gaps before enabling parallel work; do not bridge them
with private synchronization in C. Pure single-owner operations can proceed
independently while that boundary is integrated.

Full file-manager features come later under the ClearShell contract, including
lazy selection-aware copy/move and original path navigation. They are not an
excuse to expand the first runnable milestone.

4. Toolchain and known limits
----------------------------
App compiler: stg/l1_baseline/build/l1trans/gen2/l1trans.exe
  binary SHA256: 65D5A5ED127CA1BAEBDD1D500A5B74CEEA63EC1985EAC52EDEF28EFEB261C936
  recorded generated-source SHA256:
    09A06E450083825A92D201179D5F23D6E896077DC66420DD614877BCF4BBAB51
Use run_mixa.ps1; no shared gate or promotion for app-only changes.

L1 immutable grouping is available on this compiler: a direct translation
probe emits const int and const char*. It is not L2 runtime graph freezing.
See CODING_RULES section1 and L1_spec section5 for the actual C qualifiers.

const-qualified pointer spelling remains L1_spec open point10.13. The verified
app bridge is typedef const MixaBackendVTable *MixaBackendTableRef, qualified
through the alias. Do not resurrect mixa_backend_tables.c. Bad-spelling
diagnostics were fixed in root5f03a70/candidateD328DD1D; do not assume that
candidate has replaced stable65D5 without an explicit promotion record.

Header units ARE supported by stable65D5 (struct and fnptr included).
Migrating app *.h to *.h.lm1 still needs the app runner's ordered generation of
*.lm1.h and consumer includes. Ordinary-unit function-pointer spelling is a
separate limitation (10.16). No by-value header-struct local, and static linkage
support must not be invented from external:, which declares prototypes.
Reproducers are tests/l1_gaps; check against the actual chosen compiler.

5. Explicitly open product/integration choices
----------------------------------------------
- Pointer contrast and missing-glyph fallback: UI_MODEL6.3. Report the measured
  font and glyphs; do not silently change the eight-arrow specification.
- Focus-driven simultaneous horizontal/vertical scrolling: UI_MODEL5.7.2.
- Visual inspection of the overlay on square buttons; tests do not settle taste.
- App integration of the current Message prototype: PROCESS_SEAM7. Core API
  existence is not app adoption or a stable self-hosted L2 implementation.

6. Starting a session
--------------------
Read README and PORT_OF_CLEARSHELL, then the relevant subject doc. Read the
latest assigned ticket and reply, inspect the shared worktree, and preserve
ownership. Keep one current status rather than appending contradictory updates.
Keep agreed examples and source evidence. Commit/push documentation separately
after review; source checkpoints require their own relevant verification.
