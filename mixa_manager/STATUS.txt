Mixa Manager — L2 port status
==============================
Rewritten 2026-09-14 (runner-uniformity ticket, item 4). Replaces the
2026-09-10 ClearShell-handoff narrative below the table (superseded by
this whole L2-port effort; the ClearShell UI/behavior contract itself
is unaffected and still lives in the subject docs — see README.txt).
Replaces the 51 separate mixa_*_l2_port.txt prose files as the single
current-state reference for the clean-L2 port; those files still hold
each module's own investigation narrative and stay in git history for
that reason, but this table is the one place to check current status.

Owners (Mikhail's ruling, 2026-09-14): authority follows the model —
lingvamyxa-e2 (Fable) coordinates and assigns work across all three
lanes; lingvamyxa-d6 (Opus) owns the core L2 compiler/runtime and the
integration branch; this chat (Sonnet) owns mixa_manager, the app
port. Codex, OpenCode, and grok_bot are no longer active on this
project — ignore any older document naming them as current owners.

How to read the table
----------------------
Columns: module | commit (introduced the .lm2) | .lm1->.lm2 diff lines
| known-barrier bucket | oracle checks | current parity verdict.

Verdict values: PASS (full L2 translation + build + run, byte-
identical trace to the oracle — a real completed port), EXPECTED_CORE_
BARRIER (translation stops at an already-cataloged compiler gap, not a
pass), UNEXPECTED_FAILURE (stops at something else — sometimes a real,
newly-surfaced compiler gap; sometimes a harness/test bug on this
side; not yet triaged for every row below), PARITY_FAILURE (both
sides built and ran but traces differ — none currently).

Every verdict below was run fresh on 2026-09-14 against the current
pinned compiler (722AC86E..., post Stage A bare-name resolution),
not carried over from an older snapshot. The barrier-bucket column is
the LAST diagnostic actually recorded for that module in its own port
note or in this session's own work; for modules whose first stop has
since moved (the ulong-domain and Stage-A work landing this week), the
verdict column is current but the bucket text may describe an earlier
stop — flagged inline with "(moved)" wherever this session directly
observed the shift today. Treat a bucket without that flag as
"probably still accurate, not re-confirmed against today's exact
line:col."

module                | commit   | diff | barrier bucket                              | checks | verdict
-----------------------|----------|------|-----------------------------------------------|--------|------------------------
app_controller          | 12aa8b44 | 15   | incompatible entry signature (const-pointer return of a foreign struct) | 33/33 | EXPECTED_CORE_BARRIER
app_fmpanel             | 8cd4249c | 45   | Stage A fixed (mixa_selection_deselect prototype); now clean | 39/39 | PASS
app_main                | 02f66d0a | 50   | incompatible entry signature (no ABI probe by design; build-only, no trace diff) | n/a | EXPECTED_CORE_BARRIER
app_panel                | b1b55c2e | 3    | unknown foreign type (moved past; now clean) | 46/46 | PASS
app_path                | 8b9496f5 | 27   | fixed this session (RuntimeTrio + unconditional L2_RUNTIME_ROOT); now clean | n/a | PASS
app_win32                | 29539c99 | 3    | unknown foreign type -> c.sizeof(wchar_t) -> unsupported index at 211:5 "buf[off[0]]", all fixed by d6's Stage B -- now translates completely (exit 0), but three NEW gcc-level codegen bugs in the generated C: an undeclared temp (l2_q24, should be l2_q26 -- same shape as share's l2_q12/l2_q13), memcmp called with too few arguments (same shape as calculator_syntax), and "expected expression before ')' token" -- real compiler gaps, new, not yet reported | 58/58 | UNEXPECTED_FAILURE
app_window               | e7ab258d | 16   | harness bug fixed this session (scalar-field[0]); now translates and compiles fully after Stage B step 1, but fails to LINK: multiple definition of mixa_rect_put_text/mixa_rect_dump between L2Deps mixa_buttons and mixa_draw (own table config issue, mine to fix, not yet fixed) | 162/162 | UNEXPECTED_FAILURE
audio_button             | 7d4497df | 2    | RuntimeTrio was $false in the table (missing lmx_array_ref_owned.lm1.h at compile); flipped to $true -- now a genuine PASS, trace byte-identical | 33/33 | PASS
audio                    | 1ecd490c | 2    | unknown foreign type, fixed by Stage B step 1 -- now translates/builds/runs, but a REAL behavioral bug: L2 side reports "closes=0 want 1" (1/85 checks fail), not a compiler gap, mine to debug | 85/85 | PARITY_FAILURE
audio_launch             | f227ca6d | 2    | unknown foreign type | 45/45 | PASS
audio_mp3                | (own port) | 2  | import-cap gone on 722AC86E; now clean | 36/36 | PASS
audio_panel              | (own port) | 2  | Stage A fixed; ulong -- re-verified after d6's ulong fix: translates, builds and runs clean, trace byte-identical to the oracle | 45/45 | PASS
audio_scan               | (own port) | 2  | unknown foreign type (moved past) | 16/16 | PASS
audio_win32               | (own port) | 3 | unknown foreign type | 30/30 | EXPECTED_CORE_BARRIER
backend_ctors_headless   | (own port) | 3  | incompatible entry signature | 5/5 | EXPECTED_CORE_BARRIER
backend_ctors_win32      | (own port) | 3  | incompatible entry signature | 7/7 | EXPECTED_CORE_BARRIER
backend_headless         | (own port) | 4  | incompatible entry signature | n/a | EXPECTED_CORE_BARRIER
backend_table            | (own port) | 4  | incompatible entry signature | n/a | EXPECTED_CORE_BARRIER
backend_win32            | (own port) | 4  | ulong own-array (not convertible to shared engine cleanly; own script permanently) | n/a | EXPECTED_CORE_BARRIER
button_dispatch          | (own port) | 32 | incompatible entry signature | n/a | EXPECTED_CORE_BARRIER
buttons                  | (own port) | 769| unknown foreign type (moved past) | n/a | PASS
calculator_syntax        | (own port) | 2  | "a callable field needs a method name" at 89:1, fixed by d6's d24373ca; RuntimeTrio was $false in the table, flipped to $true -- now translates, but a NEW gcc-level codegen bug: memcmp called with too few arguments (real compiler gap, new, not yet reported) | n/a | UNEXPECTED_FAILURE
cmdline_dispatch         | (own port) | 36 | now stops at 130:9, "unsupported body" -- re-verified after Stage B steps 1/2a, not yet characterized in detail | n/a | UNEXPECTED_FAILURE
cmdline                  | (own port) | 21 | incompatible entry signature (moved past) | n/a | PASS
composite_glyphs         | (own port) | 58 | harness bug fixed this session; real stop below unexplored | n/a | UNEXPECTED_FAILURE
composite                | (own port) | 69 | incompatible entry signature | n/a | EXPECTED_CORE_BARRIER
console_window           | (own port) | 44 | unknown foreign type | n/a | EXPECTED_CORE_BARRIER
copy                     | (own port) | 5  | incompatible entry signature (moved) | n/a | EXPECTED_CORE_BARRIER
dir_win32                | (own port) | 767| unknown foreign type at 54:34, fixed by Stage B step 1 -- now stops at 69:5 "unsupported own array declaration"; table's own BarrierSet was 'UI' (didn't recognize array-declaration stops as expected) rather than 'UIA', fixed to match | n/a | EXPECTED_CORE_BARRIER
draw                     | (own port) | 83 | harness bug fixed this session; real stop below unexplored | n/a | UNEXPECTED_FAILURE
event_fifo               | (own port) | n/a| incompatible entry signature (moved past) | n/a | PASS
file_manager             | (own port) | 5  | unknown method fixed (e22f8bbf); now RuntimeTrio import unresolved (moved, not migrated) | n/a | UNEXPECTED_FAILURE
file_win32               | (own port) | 21 | unknown foreign type -> ulong own-array -> own array of long, still at 173:5, "[]: long hi 1" -- re-verified after Stage B steps 1/2a, unchanged: a real compiler gap, same family as the ulong one, just signed; table's own BarrierSet was 'UI', fixed to 'UIA' to match audio_win32/share_win32's classification of the same barrier text | n/a | EXPECTED_CORE_BARRIER
fileio_win32             | (own port) | 4  | unknown foreign type at 20:64 (c.sizeof), fixed by Stage B step 2a -- translation and compile now succeed; RuntimeTrio was $false in the table (missing lmx_array_owned.lm1.h at compile), flipped to $true -- now a genuine PASS, trace byte-identical | n/a | PASS
fm_copy                  | (own port) | 5  | unknown foreign type (moved past) | n/a | PASS
fm_remove                | (own port) | 4  | unknown foreign type (moved past) | n/a | PASS
help                     | (own port) | 37 | Stage A fixed (RuntimeTrio); now clean | n/a | PASS
highlight                | (own port) | 773| harness bug fixed this session; real stop below unexplored | n/a | EXPECTED_CORE_BARRIER
pointer                  | (own port) | 19 | unknown foreign type | n/a | EXPECTED_CORE_BARRIER
process_marker           | (own port) | 27 | unsupported body at 30:5 (LARGE_INTEGER local) -> c.sizeof(LARGE_INTEGER) -> unsupported cast type at 212:71, all three fixed by d6's Stage B (06a940e5, 2620d3f5, d24373ca) -- now translates, builds and runs clean, trace byte-identical to the oracle | n/a | PASS
process_win32            | (own port) | 4  | unsupported own array declaration -> unknown foreign type at 46:64, fixed by Stage B step 1 -- now translates/builds/runs; RuntimeTrio was $false in the table (missing lmx_array_owned.lm1.h at compile), flipped to $true, but 5 checks now fail for real: stdin-disabled exit code, EOF reporting, echo round trip, QUIT write, exit-code capture -- a real stdin/pipe bug in the port, not a compiler gap, mine to debug | n/a | PARITY_FAILURE
pump                     | (own port) | 3  | unknown foreign type (moved past) | n/a | PASS
remove_confirm           | (own port) | 14 | unknown field path root at 91:13 -- re-verified after Stage B steps 1/2a, unchanged (real compiler gap, d6's, confirmed via direct repro this session) | n/a | UNEXPECTED_FAILURE
remove                   | (own port) | 3  | incompatible entry signature (moved) | n/a | EXPECTED_CORE_BARRIER
selection                | (own port) | 41 | incompatible entry signature (moved past) | n/a | PASS
selection_walk           | (own port) | 4  | incompatible entry signature (moved) | n/a | EXPECTED_CORE_BARRIER
share_action             | (own port) | 5  | Stage A fixed (mixa_selection_deselect prototype); other stop remains | n/a | UNEXPECTED_FAILURE
share_button              | (own port) | 4  | unsupported own array declaration, fixed by Stage B step 1 -- now translates/builds/runs; RuntimeTrio was $false in the table (missing lmx_array_owned.lm1.h at compile), flipped to $true, but a real trace diff remains (PARITY_FAILURE), not yet characterized in detail | n/a | PARITY_FAILURE
share                    | (own port) | 4  | unknown foreign type -> unsupported index at 110:17 "buf[off[0]]", both fixed by d6's Stage B; RuntimeTrio was $false in the table, flipped to $true -- now translates, but a NEW gcc-level codegen bug: undeclared temp l2_q12 (should be l2_q13), same shape as app_win32's l2_q24/l2_q26 (real compiler gap, new, not yet reported) | n/a | UNEXPECTED_FAILURE
share_win32               | (own port) | 3  | unsupported own array declaration | n/a | EXPECTED_CORE_BARRIER
text_rect                | (own port) | 351| harness bug fixed this session; unknown foreign type, fixed by d6's Stage B; RuntimeTrio was $false in the table, flipped to $true -- now translates. The [0]-indexed field writes (p\fg[0] etc) were NOT a translator regression as first suspected: checked mixa_tiles_l2.h.lm1's own generated C directly, MixaCellByte fg/bg/flags/width/alpha are plain scalar fields (`MixaCellByte fg;`), not 1-element arrays -- the .lm2's own header comment describing them as arrays was wrong (stale from an earlier representation, or just never checked). Fixed by dropping the [0] index (10 occurrences); now a genuine PASS, trace byte-identical | n/a | PASS
tiles                    | (own port) | 54 | harness bug fixed this session; now translates past its earlier barrier; same [0]-index-on-a-scalar-field bug as text_rect (5 occurrences, confirmed against mixa_tiles_l2.h.lm1's own struct), same fix, same root cause (not a translator issue) -- now a genuine PASS, trace byte-identical | n/a | PASS

Parameterized runner (run_mixa_l2_parity.ps1 -Module <name>)
--------------------------------------------------------------
35 of the modules above are driven by the single parameterized runner
(mixa_manager/run_mixa_l2_parity.ps1 + l2_parity_module_table.ps1)
instead of their own run_mixa_<module>_l2_parity.ps1 script: app_path,
audio_button, audio, audio_launch, audio_panel, audio_scan,
audio_win32, calculator_syntax, dir_win32, file_win32, fileio_win32,
pointer, process_marker, process_win32, share, text_rect, event_fifo,
cmdline, pump, audio_mp3, fm_copy, fm_remove, buttons, help,
app_window, backend_ctors_headless, backend_headless, backend_table,
copy, remove, share_button, share_win32, remove_confirm,
selection_walk, cmdline_dispatch.

Kept as their own script permanently, by explicit decision (cut
complexity rather than model every bespoke shape in the table):
app_main (build-only, no second execution, no trace diff -- different
control flow, not just different data), app_controller (array-shaped
link-argument list instead of every other runner's inline string),
app_panel (real .lnk-fixture WScript.Shell COM setup + a test-owned
oracle-source override -- genuinely bespoke, not worth the schema
cost), backend_win32 (no runtime-object-link block, no L1Root
push-location for headers, different -I flags -- not a copy-paste-
with-a-missing-line case).

Not yet migrated (batch 4, waiting on d6's ulong translator commit so
it lands as one pass with the seven ulong re-runs, per e2): button_
dispatch, composite, composite_glyphs, console_window, draw, highlight
(inline non-Build-RealDep dependency-build patterns, not yet verified
against the parameterized engine's assumptions), selection (needs a
new dependency shape -- a plain-C file, tests/mixa_selection_alloc_
fault.c, compiled directly without an l1trans translation step first,
which the engine doesn't support yet), tiles (same inline-pattern
question as button_dispatch/draw/etc).

Known real compiler gaps (not app-side bugs, do not attempt to work
around; owned by lingvamyxa-d6)
--------------------------------------------------------------
- wchar_t anywhere in a foreign-type position (4 syntactic shapes
  confirmed 2026-09-13): FIXED by d6's Stage B step 2a (2620d3f5,
  "c.sizeof type operands as written"). fileio_win32 and app_win32's
  own c.sizeof(wchar_t) stops both cleared; re-verified directly.
- ulong as an own-array element type: translator half landed. Own
  arrays of OTHER element types (signed long: file_win32 173:5;
  uchar/others: audio_win32, share_win32, dir_win32) remain a real
  gap ("unsupported own array declaration") -- same message for every
  element type, so the barrier classification (BarrierSet 'UIA' in
  l2_parity_module_table.ps1) treats them as one known bucket; the
  fix itself may or may not be, d6's call.
- "unsupported index" on a bracket-subscript through an out-parameter
  (app_win32 211:5 "buf[off[0]]", share 110:17) -- new, found this
  session re-verifying against Stage B steps 1/2a, not yet reported.
- "unsupported cast type" (process_marker 212:71, frame=cast) -- new,
  found the same way, not yet reported.
- "a callable field needs a method name" (calculator_syntax 89:1,
  frame=prototype) -- new, not yet reported.
- remove_confirm: "unknown field path root" on a declared-typed
  global's own field access after a cast-assignment inside a nested
  if -- confirmed via a direct, isolated repro 2026-09-13; re-verified
  unchanged after Stage B steps 1/2a.
- file_manager's former "unknown method" stop (call through a
  function-pointer-typed local variable) was fixed by d6's commit
  e22f8bbf. The module now stops instead on its own harness's L2
  import root: l1trans error: cannot read import l2src/
  lmx_array_owned.h.lm1 -- the standalone run_mixa_file_manager_
  l2_parity_module_table.ps1 predates the parameterized engine's
  RuntimeTrio wiring and isn't migrated yet (see "Not yet migrated"
  above); this is not a new compiler gap.

Real app-side bugs found once compiler barriers cleared (mine, not
d6's -- not yet debugged)
--------------------------------------------------------------
- audio: L2 side reports "closes=0 want 1" (1/85 checks fail).
- process_win32: 5/many checks fail, all stdin/pipe-shaped -- stdin-
  disabled exit code, EOF reporting, echo round trip, QUIT write,
  exit-code capture.
- share_button: a real trace diff, not yet characterized in detail.

Recorded debt
--------------
- mixa_console_window.c is hand-written C living inside mixa_manager.
  CODING_RULES.txt section 0 forbids this -- application logic belongs
  in L1 (moving to L2), never hand-written C, with narrow platform-ABI
  adapters as the only identified exception and this is not one.
  mixa_console_read_window's own body needs porting to mixa_manager/
  mixa_console_window.lm1 itself. Not fixed here; flagged for whoever
  picks up console_window's own batch-4 work, since its own runner
  already predefs/compiles this file and would need to stop doing so
  once the port lands. Owner: mixa_manager (this chat).

Toolchain
----------
Pinned stable L1 translator: stg/l1_baseline/build/l1trans/gen2/
l1trans.exe and build/l1trans/gen3/l1trans.exe, both read from a
single source of truth: stg/l1_baseline/l2src/L1_PIN.txt (one line,
the pin's SHA256, nothing else -- a promotion is now a one-line change
to that file, not the historical 73 separate literal-hash edits).
Current pin (as of this table): 722AC86E256D28EB462EE244D92B5E7188792
EC0A0F5B300957622672EBAB466. mixa_manager/lib_l2_runtime_support.ps1
exposes Get-L1Pin/Assert-PinnedL1Translator for any script that needs
to check it.

Everything below this line is the 2026-09-10 ClearShell-handoff
snapshot, kept for historical continuity only -- it predates the L2
port effort above and its own owner references (Codex/grok_bot) are
stale; see the table above for current status and the Owners section
above for current ownership.
==============================================================

Task24/25 COMMITTED AND PUSHED: 75ca805.
  Tiles/buttons, per-handle backend dispatch, L1 constructor registries,
  concurrent backend handles, even physical cells and shared text/UI font.
  The handwritten C registry was removed; its typedef bridge remains in the
  permitted ABI header. 2a70b34 strengthens negative L1 diagnostic fixtures.

Task26 REVIEWED, NOT YET COMMITTED at this snapshot.
  One moving double-box frame, general MIXA_DRAW_KEEP_BG, snapshot restoration.

Task27 LOGIC REVIEWED; corrective checkpoint 080500 accepted, not committed.
  Eight last-displacement octants; no smoothing/latch; initial down-right.
  The current selected Windows pointer font was measured as MS Gothic,
  tmHeight18/ascent15. All eight arrow glyph indices were absent (0xFFFF).
  No fallback has been chosen.

3. Next work (as of 2026-09-10, superseded by the L2 port effort above)
------------------------------------------------------------------------
Correct the bounded backend error checks and report glyph/fallback choices.
Implement the PROCESS_SEAM for FIRST_VERSION mode0.1.2. Implement jobs and
asynchronous system-event handoffs through the Message boundary (PROCESS_SEAM6).
Full file-manager features come later under the ClearShell contract.

4. Explicitly open product/integration choices (as of 2026-09-10)
--------------------------------------------------------------------
- Pointer contrast and missing-glyph fallback: UI_MODEL6.3.
- Focus-driven simultaneous horizontal/vertical scrolling: UI_MODEL5.7.2.
- Visual inspection of the overlay on square buttons; tests do not settle taste.
- App integration of the current Message prototype: PROCESS_SEAM7.

5. Starting a session
--------------------
Read README and PORT_OF_CLEARSHELL, then the relevant subject doc. Read the
latest assigned ticket and reply, inspect the shared worktree, and preserve
ownership. Keep one current status rather than appending contradictory updates.
Commit/push documentation separately after review; source checkpoints require
their own relevant verification.
